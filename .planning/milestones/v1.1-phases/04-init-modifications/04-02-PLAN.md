---
phase: 04-init-modifications
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/research/discover/SKILL.md
  - .claude/commands/research/start-phase/SKILL.md
autonomous: true
requirements:
  - DISC-04
  - INIT-02

must_haves:
  truths:
    - "The discover skill checks for research/discovery/strategy.md before falling back to type-channel map keyword matching"
    - "If strategy.md exists, the discover skill reads pre-matched phase-to-channel mappings directly without keyword guessing"
    - "If strategy.md does not exist, the discover skill falls back to reading the type-channel map (existing behavior unchanged)"
    - "The start-phase skill output recommends running /research:discover as the first step"
  artifacts:
    - path: ".claude/commands/research/discover/SKILL.md"
      provides: "Updated discover skill with strategy.md priority check"
      contains: "strategy.md"
    - path: ".claude/commands/research/start-phase/SKILL.md"
      provides: "Updated start-phase skill recommending discover"
      contains: "research:discover"
  key_links:
    - from: ".claude/commands/research/discover/SKILL.md"
      to: "research/discovery/strategy.md"
      via: "Pre-check reads strategy.md first if it exists"
      pattern: "strategy.md"
    - from: ".claude/commands/research/start-phase/SKILL.md"
      to: ".claude/commands/research/discover/SKILL.md"
      via: "Output text recommends /research:discover"
      pattern: "research:discover"
---

<objective>
Update the discover skill to prioritize strategy.md over type-channel map keyword matching, and update start-phase to recommend running discover.

Purpose: Strategy.md (produced by init) provides pre-matched phase-to-channel mappings. The discover skill should use this "express lane" when available, falling back to type-channel map keyword matching for projects not scaffolded with init. Start-phase should surface the discover recommendation at the natural decision point.

Output: Modified `discover/SKILL.md` and `start-phase/SKILL.md`.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-init-modifications/04-CONTEXT.md
@.planning/phases/03-discover-skill/03-01-SUMMARY.md

<interfaces>
<!-- Discover skill Pre-check step 4 (the section to modify): -->

From .claude/commands/research/discover/SKILL.md (Pre-check steps 3-5):
```
3. **Determine the research type** — read the project's CLAUDE.md. Find the `research-type` field.

4. **Read the type-channel map** at `.claude/reference/discovery/type-channel-maps/{research-type}.md`. Parse `active-channels` from the frontmatter. Find the Discovery Group whose Phases list matches (or partially matches, via keyword) the current active phase name. The matched Discovery Group determines which channels to query and their priority order (Primary, Secondary).

5. **If no Discovery Group matches the current phase**, report: "No discovery channels mapped for this phase — this phase uses existing sources." and stop.
```

From .claude/commands/research/start-phase/SKILL.md (Output section):
```
**Ready to begin:** Start collecting sources with `/research:process-source` for Phase [N] questions only.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strategy.md priority path to discover skill</name>
  <files>.claude/commands/research/discover/SKILL.md</files>
  <action>
Read the full discover skill at `.claude/commands/research/discover/SKILL.md`. Modify Pre-check step 4 to add a strategy.md priority check BEFORE the existing type-channel map lookup. The existing step 4 becomes the fallback.

Replace step 4 with this two-part logic:

**4a. Check for discovery strategy** — read `research/discovery/strategy.md` if it exists. If found, look up the current active phase name in the strategy file. The strategy contains pre-matched phase-to-channel mappings (primary and secondary channels per phase). If the current phase is found in the strategy:
- Use the pre-matched channels directly — no keyword guessing needed
- Print: "Using project discovery strategy (pre-matched channels)"
- Skip to step 5 check (if channels list is empty or marked "no discovery", report "No discovery channels mapped for this phase" and stop)

**4b. Fall back to type-channel map** (only if strategy.md does not exist OR the current phase was not found in strategy.md):
- This is the EXISTING step 4 logic, unchanged: Read the type-channel map at `.claude/reference/discovery/type-channel-maps/{research-type}.md`, parse active-channels, keyword-match the Discovery Group
- Print: "Using type-channel map (keyword matching)"

Keep step 3 (determine research type) as-is — it is still needed for the fallback path. Keep step 5 (no match handling) as-is.

Do NOT change any other part of the discover skill. The channel execution, candidate output format, guardrails, and all other sections remain unchanged.
  </action>
  <verify>
    <automated>grep -q "strategy.md" .claude/commands/research/discover/SKILL.md && echo "PASS: strategy.md referenced" || echo "FAIL: strategy.md missing"; grep -q "pre-matched" .claude/commands/research/discover/SKILL.md && echo "PASS: pre-matched path present" || echo "FAIL: pre-matched path missing"; grep -q "type-channel-map" .claude/commands/research/discover/SKILL.md && echo "PASS: fallback preserved" || echo "FAIL: fallback missing"</automated>
  </verify>
  <done>
    - Discover skill Pre-check step 4 checks strategy.md first
    - If strategy.md exists and has the phase, uses pre-matched channels directly
    - If strategy.md missing or phase not found, falls back to type-channel map keyword matching (existing behavior)
    - No other sections of the discover skill are changed
  </done>
</task>

<task type="auto">
  <name>Task 2: Add discover recommendation to start-phase skill</name>
  <files>.claude/commands/research/start-phase/SKILL.md</files>
  <action>
Read the full start-phase skill at `.claude/commands/research/start-phase/SKILL.md`. Make one targeted change to the Output section.

Replace the "Ready to begin" line:
```
**Ready to begin:** Start collecting sources with `/research:process-source` for Phase [N] questions only.
```

With:
```
**Recommended first step:** Run `/research:discover` to find candidate sources for this phase's questions, then process the best candidates with `/research:process-source`.
```

This is a locked decision from CONTEXT.md: "start-phase mentions discover" and "start-phase output includes: 'Recommended first step: run /research:discover to find candidate sources for this phase'"

Do NOT change any other part of the start-phase skill. The Process steps, Guardrails, and Common Failure Modes remain unchanged.
  </action>
  <verify>
    <automated>grep -q "research:discover" .claude/commands/research/start-phase/SKILL.md && echo "PASS: discover referenced" || echo "FAIL: discover missing"; grep -q "Recommended first step" .claude/commands/research/start-phase/SKILL.md && echo "PASS: recommendation present" || echo "FAIL: recommendation missing"</automated>
  </verify>
  <done>
    - Start-phase Output section recommends /research:discover as the first step
    - No other sections of start-phase are changed
  </done>
</task>

</tasks>

<verification>
1. `grep -n "strategy.md" .claude/commands/research/discover/SKILL.md` — should show strategy.md check in Pre-check section
2. `grep -n "research:discover" .claude/commands/research/start-phase/SKILL.md` — should show discover recommendation in Output section
3. `grep -c "pre-matched\|keyword" .claude/commands/research/discover/SKILL.md` — should show both paths exist (priority + fallback)
</verification>

<success_criteria>
The discover skill checks strategy.md first (express lane) and falls back to type-channel map keyword matching (existing behavior) when no strategy exists. The start-phase skill recommends running discover at the natural decision point. Both skills remain fully functional for projects with and without strategy.md.
</success_criteria>

<output>
After completion, create `.planning/phases/04-init-modifications/04-02-SUMMARY.md`
</output>
