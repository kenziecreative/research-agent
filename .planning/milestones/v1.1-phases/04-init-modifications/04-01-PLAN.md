---
phase: 04-init-modifications
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/research/init/SKILL.md
autonomous: true
requirements:
  - DISC-04
  - INIT-01
  - INIT-02

must_haves:
  truths:
    - "Running /research:init creates a research/discovery/ directory in the project scaffold"
    - "The plan-generator subagent produces research/discovery/strategy.md mapping phases to channels"
    - "The CLAUDE.md template includes /research:discover in the skills table with trigger and job description"
    - "The CLAUDE.md template workflow section mentions discovery as the first Collect substep"
    - "The CLAUDE.md enforcement section includes a soft rule recommending /research:discover"
    - "The STATE.md template Collect checkbox mentions /research:discover"
    - "Step 5 verification confirms discovery/strategy.md exists"
    - "Step 6 report lists 9 skills including /research:discover"
  artifacts:
    - path: ".claude/commands/research/init/SKILL.md"
      provides: "Updated init skill with discovery scaffold, strategy generation, and CLAUDE.md/STATE.md template updates"
      contains: "research/discovery/"
  key_links:
    - from: ".claude/commands/research/init/SKILL.md"
      to: ".claude/reference/discovery/type-channel-maps/"
      via: "Plan-generator subagent receives type-channel map content to produce strategy.md"
      pattern: "type-channel-map"
    - from: ".claude/commands/research/init/SKILL.md"
      to: "research/discovery/strategy.md"
      via: "Plan-generator produces strategy file in scaffold"
      pattern: "discovery/strategy.md"
---

<objective>
Update the init skill to scaffold discovery infrastructure and advertise /research:discover to new projects.

Purpose: When /research:init runs, the scaffolded project must include the discovery directory, a project-specific discovery strategy generated by the plan-generator subagent, and CLAUDE.md/STATE.md content that references the discover skill. This makes discovery a first-class part of every new research project.

Output: Modified `.claude/commands/research/init/SKILL.md` with all discovery-related additions.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-init-modifications/04-CONTEXT.md
@.planning/phases/03-discover-skill/03-01-SUMMARY.md

<interfaces>
<!-- The init skill is the sole file modified. Key integration points: -->

From .claude/commands/research/init/SKILL.md (current structure):
- Step 1: Gather Project Information (4 questions)
- Step 2: Create Directory Structure (mkdir scaffold)
- Step 3: Generate the Research Plan (Sonnet subagent produces research-plan.md)
- Step 4: Assemble and Write Files (CLAUDE.md sections 1-10, STATE.md, reference files, gaps.md, etc.)
- Step 5: Verify (ls checks, CLAUDE.md read-back, STATE.md read-back)
- Step 6: Report (directory path, research type, phase count, finding tags, 8 skills, next steps)

From .claude/reference/discovery/type-channel-maps/*.md (example frontmatter):
```yaml
---
research-type: company-for-profit
active-channels:
  - web-search
  - regulatory
  - financial
  - social-signals
  - domain-specific
---
```
Each map has Discovery Groups with Phases, Primary channels, and Secondary channels.

From .claude/commands/research/discover/SKILL.md (Pre-check step 4):
The discover skill currently reads type-channel maps at `.claude/reference/discovery/type-channel-maps/{research-type}.md` and keyword-matches phase names to Discovery Groups. The strategy.md file will provide a pre-matched alternative path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update init skill with all discovery modifications</name>
  <files>.claude/commands/research/init/SKILL.md</files>
  <action>
Read the full current init skill at `.claude/commands/research/init/SKILL.md`. Apply ALL of the following modifications in a single pass. Every change below is a locked decision from CONTEXT.md — implement exactly as specified.

**Step 2 (Directory Structure):** Add `discovery/` to the directory tree diagram under `research/`:
```
research/
├── ...existing directories...
├── discovery/               # Discovery strategy and candidate sources
├── ...
```

**Step 3 (Plan Generator):** Extend the plan-generator subagent instructions to ALSO produce `discovery-strategy.md`. After the existing plan generator instructions block (before the "Wait for the agent to complete" line), add instructions telling the subagent:

- Read the type-channel map at `.claude/reference/discovery/type-channel-maps/{research-type}.md`
- For each phase in the research plan it just generated, find the matching Discovery Group from the type-channel map (keyword match on phase name)
- Produce `research/discovery/strategy.md` with this format:
  - A header identifying the research type
  - For each phase that has a matching Discovery Group: the phase name, primary channels, and secondary channels (pre-matched, no keyword guessing needed at discovery time)
  - Phases with no matching Discovery Group are listed as "no discovery — uses existing sources"
- Write the file to `<project-root>/research/discovery/strategy.md`

The subagent already has access to the research type from its existing inputs. The new input it needs is the type-channel map content. Add an instruction for the init skill to read the type-channel map and pass its content to the subagent alongside the existing type template content.

**Step 4 (CLAUDE.md assembly):**

Section 3 (Directory Structure): Add the `discovery/` directory entry to the directory tree:
```
├── discovery/               # Discovery strategy and candidate sources
```
Place it after `reference/` and before the closing note about not creating files outside the structure.

Section 4 (Skills table): Add a new row to the skills table:
```
| Discover Sources | `/research:discover` | Finds candidate sources for the current phase using type-aware multi-channel discovery |
```
Place after the Progress row (last current row).

Section 5 (Workflow): In the phase cycle numbered list, update step 1 (Collect) to read:
"1. **Collect** — Start by running `/research:discover` to find candidate sources for the current phase, then use `/research:process-source` for each URL, PDF, or document relevant to the *current phase only*."

Also add a line after the cycle list: "`research/discovery/strategy.md` maps each phase to its highest-value discovery channels. The discover skill reads this file automatically."

Enforcement rules: Add a new bullet (after the existing enforcement rules, before the "Clear context between phases" section):
"- **Running `/research:discover` at the start of each phase is recommended but not mandatory.** It surfaces the highest-value sources for the current phase's questions via multi-channel discovery. Sources can still be found manually and processed with `/research:process-source`."

Section 10 (Reference Protocols table): No changes needed — discovery reference files live in the research-agent project, not in scaffolded projects.

**Step 4 (STATE.md template):** In the Current Phase Cycle checklist template, update the Collect line:
```
- [ ] **Collect** — Sources gathered for this phase's questions (start with /research:discover)
```

**Step 4 (Other Files):** No changes needed — discovery/strategy.md is produced by the plan-generator subagent in Step 3, not created separately.

**Step 5 (Verify):** Add to the verification list item 1 (the `ls` check):
```
- `discovery/strategy.md`
```

**Step 6 (Report):**
- Change "The eight research skills available:" to "The nine research skills available:"
- Add `/research:discover` to the skill list
- The report skill list should now include: `/research:process-source`, `/research:cross-ref`, `/research:check-gaps`, `/research:audit-claims`, `/research:summarize-section`, `/research:start-phase`, `/research:phase-insight`, `/research:progress`, `/research:discover`

**Also update** the Section 4 CLAUDE.md integrity agent line to say 9 skills where it currently says 8 (if it references a count).

Do NOT modify any other files. All changes are within init/SKILL.md.
  </action>
  <verify>
    <automated>grep -c "discovery" .claude/commands/research/init/SKILL.md | xargs test 5 -le && echo "PASS: discovery referenced 5+ times" || echo "FAIL: insufficient discovery references"; grep -q "research:discover" .claude/commands/research/init/SKILL.md && echo "PASS: /research:discover present" || echo "FAIL: /research:discover missing"; grep -q "discovery/strategy.md" .claude/commands/research/init/SKILL.md && echo "PASS: strategy.md referenced" || echo "FAIL: strategy.md missing"; grep -q "nine research skills" .claude/commands/research/init/SKILL.md && echo "PASS: nine skills count" || echo "FAIL: skill count not updated"</automated>
  </verify>
  <done>
    - init/SKILL.md Step 2 shows discovery/ in directory tree
    - init/SKILL.md Step 3 plan-generator instructions include strategy.md generation from type-channel map
    - init/SKILL.md Step 4 CLAUDE.md sections include: skills table row for /research:discover, workflow Collect step mentions discover, enforcement soft rule for discover, directory structure shows discovery/
    - init/SKILL.md Step 4 STATE.md template Collect checkbox mentions /research:discover
    - init/SKILL.md Step 5 verification includes discovery/strategy.md
    - init/SKILL.md Step 6 report lists 9 skills including /research:discover
  </done>
</task>

</tasks>

<verification>
1. `grep -n "discovery" .claude/commands/research/init/SKILL.md` — should show references in Steps 2, 3, 4, 5, and 6
2. `grep -n "research:discover" .claude/commands/research/init/SKILL.md` — should appear in skills table, workflow, enforcement, STATE.md template, and report
3. `grep -n "strategy.md" .claude/commands/research/init/SKILL.md` — should appear in Step 3 (generation), Step 4 (CLAUDE.md workflow mention), Step 5 (verification)
4. `grep -n "nine" .claude/commands/research/init/SKILL.md` — should appear in Step 6 report
</verification>

<success_criteria>
The init skill, when executed, will: (1) create research/discovery/ in the scaffold, (2) have the plan-generator subagent produce research/discovery/strategy.md with pre-matched phase-to-channel mappings, (3) include /research:discover in the CLAUDE.md skills table and workflow, (4) mention discover in the STATE.md Collect checkbox, (5) verify strategy.md exists, and (6) report 9 skills. All changes are confined to init/SKILL.md.
</success_criteria>

<output>
After completion, create `.planning/phases/04-init-modifications/04-01-SUMMARY.md`
</output>
