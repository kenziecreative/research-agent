---
phase: 09-gap-analysis-depth
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - .claude/commands/research/phase-insight/SKILL.md
  - .claude/commands/research/summarize-section/SKILL.md
  - .claude/commands/research/start-phase/SKILL.md
autonomous: true
requirements: [GAP-01, GAP-02]

must_haves:
  truths:
    - "phase-insight reads enhanced gaps.md to surface independent source counts and adjacent matches in its analysis"
    - "summarize-section warns (but does not block) when synthesizing a section with lopsided coverage"
    - "start-phase shows the phase's coverage snapshot from gaps.md (if it exists) before the user begins collecting"
  artifacts:
    - path: ".claude/commands/research/phase-insight/SKILL.md"
      provides: "Enhanced phase insight reading gaps.md for independent counts and adjacent matches"
      contains: "independent"
    - path: ".claude/commands/research/summarize-section/SKILL.md"
      provides: "Lopsided coverage advisory pre-check"
      contains: "lopsided"
    - path: ".claude/commands/research/start-phase/SKILL.md"
      provides: "Coverage snapshot display from gaps.md"
      contains: "gaps.md"
  key_links:
    - from: ".claude/commands/research/phase-insight/SKILL.md"
      to: "research/gaps.md"
      via: "reads gaps.md for independent source counts and adjacent matches"
      pattern: "gaps.md"
    - from: ".claude/commands/research/summarize-section/SKILL.md"
      to: "research/gaps.md"
      via: "reads gaps.md for lopsided coverage flags on current section"
      pattern: "lopsided"
    - from: ".claude/commands/research/start-phase/SKILL.md"
      to: "research/gaps.md"
      via: "reads gaps.md dashboard for coverage snapshot"
      pattern: "Coverage Dashboard"
---

<objective>
Integrate the enhanced gaps.md output from check-gaps into three downstream skills: phase-insight (richer analysis), summarize-section (lopsided coverage advisory), and start-phase (coverage snapshot).

Purpose: The enhanced gap analysis data is only useful if downstream skills surface it. This plan wires the new independence counts, adjacent matches, and lopsided flags into the skills that users interact with during research.

Output: Updated phase-insight, summarize-section, and start-phase SKILL.md files
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-gap-analysis-depth/09-CONTEXT.md
@.planning/phases/09-gap-analysis-depth/09-01-SUMMARY.md

@.claude/commands/research/phase-insight/SKILL.md
@.claude/commands/research/summarize-section/SKILL.md
@.claude/commands/research/start-phase/SKILL.md

<interfaces>
<!-- gaps.md format produced by enhanced check-gaps (Plan 01) -->

gaps.md structure:
```
## Coverage Dashboard
- **Total questions:** N
- **Direct coverage:** N questions (N%)
- **Lopsided (single independent source):** N questions
- **Adjacent-only matches:** N questions

### [Phase N]: [Phase Name]

#### Q: [Question text]
**Coverage:** [status] | **Independent sources:** N [LOPSIDED if 1]

**Direct sources:**
- [filename] [Source: citation] -- [evidence summary]
*Non-independent: [filename] shares origin with [other] ([origin])*

**Adjacent sources:**
- [filename]: Addresses [actual topic] rather than [phase question]
```

From summarize-section SKILL.md current pre-checks:
- Pre-check 1: cross-reference.md freshness
- Pre-check 2: gaps.md freshness
- Pre-check 3: unresolved core contradictions gate
- Pre-check 4: source staleness advisory
- Pre-check 5: counter-evidence gate (PRD Validation / Exploratory Thesis)
Next available: Pre-check 6

From Phase 8 advisory pattern:
- Advisory pre-checks warn but do not block synthesis
- Display structured list of issues, then proceed
- Silent pass when no issues found
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance phase-insight to surface independence and adjacency data from gaps.md</name>
  <files>.claude/commands/research/phase-insight/SKILL.md</files>
  <action>
Update the phase-insight SKILL.md to read and surface the enhanced gaps.md data. phase-insight is read-only — it reads gaps.md and includes the data in its output, no new data collection.

Changes to make:

**Process section — update step 4:**
- Current: "Read `research/gaps.md` for current gap status."
- New: "Read `research/gaps.md` for current gap status, including per-question independent source counts, lopsided coverage flags, and adjacent-but-not-direct matches."

**Output section — update the Questions addressed table:**
- Add an "Independent" column and an "Adjacent" column to the table
- Current: `| Question | Sources | Strength | Notes |`
- New: `| Question | Direct Sources | Independent | Adjacent | Strength | Notes |`
- Independent column shows the independent source count from gaps.md
- Adjacent column shows count of adjacent matches (0 if none)
- Strength assessment incorporates independence: a question with 3 sources but only 1 independent is Thin, not Strong
- Notes column includes lopsided flag if present: "LOPSIDED — single independent source"

**Output section — add a new subsection after "Thin areas needing more sources":**
```
**Adjacent-only matches (not counted as coverage):**
- [Question]: [N] adjacent sources — [brief explanation of what they address instead]
```
Only include this subsection if any questions have adjacent-only matches.

**Guardrails — update guardrail 2:**
- Current: "'Thin' means one source. 'Unsupported' means zero sources."
- New: "'Thin' means one **independent Direct** source. 'Unsupported' means zero **Direct** sources. Adjacent sources do not count toward strength."

**Common Failure Modes — add one row:**
| Counting adjacent sources toward coverage strength | Adjacent sources address related topics, not the specific question. Read gaps.md match classifications — only Direct matches count. A question with 3 Adjacent and 0 Direct sources is Unsupported, not Strong. |
  </action>
  <verify>
    <automated>grep -q "Independent" .claude/commands/research/phase-insight/SKILL.md && grep -q "Adjacent" .claude/commands/research/phase-insight/SKILL.md && grep -q "independent Direct" .claude/commands/research/phase-insight/SKILL.md && grep -q "lopsided\|LOPSIDED" .claude/commands/research/phase-insight/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>phase-insight output table includes Independent and Adjacent columns; strength assessment uses independent Direct count; adjacent-only matches displayed in dedicated subsection; guardrail updated for independence-aware strength labels.</done>
</task>

<task type="auto">
  <name>Task 2: Add lopsided coverage advisory to summarize-section and coverage snapshot to start-phase</name>
  <files>.claude/commands/research/summarize-section/SKILL.md, .claude/commands/research/start-phase/SKILL.md</files>
  <action>
Two targeted additions: a pre-check in summarize-section and a coverage snapshot in start-phase.

**summarize-section changes:**

Add pre-check 6 after existing pre-check 5 (counter-evidence gate). Follow the advisory pattern from Phase 8 (warn but do not block):

```
6. **Lopsided coverage advisory.** Read `research/gaps.md` and find the questions relevant to this section. If any question has a lopsided coverage flag (only 1 independent Direct source), display an advisory:

   ```
   Lopsided coverage advisory:

   - Q: [question text] — 1 independent source ([source-note-filename])
   - Q: [question text] — 1 independent source ([source-note-filename])

   These questions are supported by a single independent data point. Findings from these questions will be flagged with "single source suggests" language per guardrail 5. Consider running /research:discover to find additional independent sources before synthesis.
   ```

   This is a **warning, not a gate** — synthesis proceeds after displaying the advisory. If no questions have lopsided coverage, skip the advisory silently.
```

Update the "If any pre-check fails" paragraph at the end of Pre-checks to note that pre-checks 4 and 6 are advisories that do not block.

Add one Common Failure Mode row:
| Synthesizing lopsided questions with confident language | Check gaps.md for lopsided flags on this section's questions. If a question has only 1 independent source, use "single source suggests" language — not "evidence shows" or "research confirms." |

**start-phase changes:**

Add step 5b after existing step 5a (assumptions). This is read-only, consistent with start-phase's no-write policy:

```
5b. **Read `research/gaps.md`** (if it exists) and extract the Coverage Dashboard for the current phase. If gaps.md has been generated for any prior phases, show a coverage snapshot so the user can see existing coverage status and lopsided flags before deciding what to collect.
```

Update the Output section — add a new subsection after "Gaps carried forward" and before "Open assumptions to revisit":

```
**Coverage snapshot** (from last gap check):
- Total questions: N | Direct coverage: N | Lopsided: N | Adjacent-only: N
- [If lopsided flags exist] Questions with only 1 independent source: [list question text]
- [If adjacent-only matches exist] Questions with only adjacent matches (not counted as covered): [list question text]
- [If gaps.md does not exist] No gap check has been run yet. Run `/research:check-gaps` after processing sources.
```

Add guardrail 6:
"6. If gaps.md exists, always show the coverage snapshot. Do not skip it even if coverage looks adequate — lopsided flags and adjacent-only matches are not obvious from source counts alone."

Add one Common Failure Mode row:
| Skipping coverage snapshot when gaps.md exists | Always read and display gaps.md coverage data if the file exists. Lopsided coverage and adjacent-only matches are invisible without it — the user needs this to decide what sources to pursue. |
  </action>
  <verify>
    <automated>grep -q "Lopsided coverage advisory" .claude/commands/research/summarize-section/SKILL.md && grep -q "pre-check 6\|Pre-check 6" .claude/commands/research/summarize-section/SKILL.md && grep -q "Coverage snapshot" .claude/commands/research/start-phase/SKILL.md && grep -q "gaps.md" .claude/commands/research/start-phase/SKILL.md && grep -q "5b" .claude/commands/research/start-phase/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>summarize-section has pre-check 6 (lopsided coverage advisory, warn-not-block pattern); start-phase has step 5b reading gaps.md and a Coverage snapshot section in output. Both follow the advisory-not-blocking pattern consistent with Phase 8.</done>
</task>

</tasks>

<verification>
1. phase-insight output table has Independent and Adjacent columns
2. phase-insight strength assessment uses independent Direct source count
3. summarize-section has pre-check 6 for lopsided coverage advisory
4. summarize-section pre-check 6 is advisory (warns, does not block)
5. start-phase has step 5b reading gaps.md
6. start-phase output includes Coverage snapshot section
7. All three skills reference gaps.md for the enhanced data
</verification>

<success_criteria>
- phase-insight surfaces independent source counts and adjacent matches from gaps.md in its output
- summarize-section warns on lopsided coverage before synthesis (advisory, not gate)
- start-phase displays coverage snapshot from gaps.md when available
- All integrations are consistent with the advisory-not-blocking pattern from Phase 8
- Adjacent matches are presented as advisory information in all three skills — never counted as coverage
</success_criteria>

<output>
After completion, create `.planning/phases/09-gap-analysis-depth/09-02-SUMMARY.md`
</output>
