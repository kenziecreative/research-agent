---
phase: 10-system-health-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/research/progress/SKILL.md
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "User can see infrastructure health status at the top of progress output, before the phase status table"
    - "Any infrastructure failure is surfaced as a named failure with a description of what is missing and why it matters"
    - "When all checks pass, output is a compact single line showing 5/5 checks passed"
    - "When checks fail, only failures are listed — passing checks are not enumerated"
    - "Next action prioritizes fixing infrastructure when health failures exist"
  artifacts:
    - path: ".claude/commands/research/progress/SKILL.md"
      provides: "Progress skill with infrastructure health checks"
      contains: "Infrastructure Health"
  key_links:
    - from: ".claude/commands/research/progress/SKILL.md"
      to: ".claude/settings.json"
      via: "Read to verify hooks structure"
      pattern: "settings\\.json"
    - from: ".claude/commands/research/progress/SKILL.md"
      to: ".claude/reference/"
      via: "Glob to verify reference file presence"
      pattern: "reference/"
    - from: ".claude/commands/research/progress/SKILL.md"
      to: "research/STATE.md"
      via: "Read to verify YAML frontmatter structure"
      pattern: "STATE\\.md"
---

<objective>
Add infrastructure health checks to the /research:progress skill so the user sees system health status before project status.

Purpose: Catch configuration drift (missing hooks, invalid JSON, missing reference files) before it silently undermines a research session. Consistent with the advisory pattern used in Phase 8 (staleness) and Phase 9 (lopsided coverage) — warn and continue.

Output: Updated progress/SKILL.md with 5 hardcoded health checks displayed before the phase status table.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-system-health-visibility/10-CONTEXT.md

<interfaces>
<!-- Current progress skill structure — executor modifies this file -->

From .claude/commands/research/progress/SKILL.md:
```markdown
---
name: progress
description: Show research project dashboard — phase status, source counts, and next action
allowed-tools: Read, Grep, Glob
---

## Process
1. Read STATE.md for current position
2. Count files in research directories
3. Read research-plan.md for phase list
4. Read cross-reference.md and count patterns
5. Read gaps.md for blocking gaps

## Output
### Research Progress
[phase table, source counts, blocking issues, next action]
```

From .claude/settings.json (read target for hook verification):
```json
{
  "hooks": {
    "PreToolUse": [
      { "matcher": "Write", "hooks": [{ "type": "command", ... }] },
      { "matcher": "Edit", "hooks": [{ "type": "command", ... }] }
    ],
    "PreCompact": [
      { "matcher": "", "hooks": [{ "type": "command", ... }] }
    ]
  }
}
```

From .claude/reference/ (glob target for file presence):
```
coverage-assessment-guide.md
discovery/
evidence-failure-modes.md
pattern-recognition-guide.md
source-assessment-guide.md
templates/
tools-guide.md
writing-standards.md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add infrastructure health check process steps to progress skill</name>
  <files>.claude/commands/research/progress/SKILL.md</files>
  <action>
Add a new step 1 to the Process section (renumber existing steps 1-5 to 2-6) that runs 5 infrastructure health checks. The new step 1 should be titled "Run infrastructure health checks" and contain these sub-steps:

**1a. Hooks active** — Read `.claude/settings.json` and verify the JSON contains:
  - A `PreToolUse` entry with `matcher: "Write"`
  - A `PreToolUse` entry with `matcher: "Edit"`
  - A `PreCompact` entry (any matcher)
  - Failure name: "PreToolUse Write hook missing" / "PreToolUse Edit hook missing" / "PreCompact hook missing"
  - Failure description explains WHY: "this hook prevents unaudited writes to outputs/" / "this hook prevents unaudited edits to outputs/" / "this hook ensures STATE.md is updated before context compacts"

**1b. JSON valid** — Read `.claude/settings.json` and confirm it parses as valid JSON with a `hooks` key containing `PreToolUse` and `PreCompact` arrays.
  - Failure name: "settings.json invalid or missing"
  - Failure description: "the settings file must be valid JSON with hooks.PreToolUse and hooks.PreCompact arrays — without it, no audit gates are active"

**1c. STATE.md structurally sound** — Read `research/STATE.md` and confirm YAML frontmatter exists (delimited by `---`) containing the keys `milestone`, `status`, and `progress`.
  - Failure name: "STATE.md missing required frontmatter"
  - Failure description: "STATE.md must have YAML frontmatter with milestone, status, and progress fields — these drive the progress dashboard"
  - If research/STATE.md does not exist, this check passes silently (no active research project)

**1d. Reference files present** — Glob `.claude/reference/` for these specific items: `coverage-assessment-guide.md`, `evidence-failure-modes.md`, `pattern-recognition-guide.md`, `source-assessment-guide.md`, `tools-guide.md`, `writing-standards.md`, `templates/` directory.
  - Failure name: "[filename] missing from .claude/reference/"
  - Failure description: "reference files guide source assessment, evidence evaluation, and output formatting — missing files degrade research quality"

**1e. Discovery strategy present** — When `research/` directory exists (active research project), check for a file matching `research/discovery-strategy*.md` or `research/discovery/*.md`.
  - Failure name: "discovery strategy missing"
  - Failure description: "a discovery strategy guides source finding — without one, source selection is ad-hoc"
  - If no research/ directory exists, this check passes silently

Collect all check results into a list of failures (empty list = all pass). Keep the skill's allowed-tools as Read, Grep, Glob — all checks use these tools only (no Bash needed).
  </action>
  <verify>
    <automated>grep -c "health check" .claude/commands/research/progress/SKILL.md | grep -q "[1-9]" && grep -q "settings.json" .claude/commands/research/progress/SKILL.md && grep -q "STATE.md.*frontmatter" .claude/commands/research/progress/SKILL.md && grep -q "reference" .claude/commands/research/progress/SKILL.md && grep -q "discovery" .claude/commands/research/progress/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Progress skill Process section has a new step 1 with 5 sub-checks (hooks, JSON, STATE.md, reference files, discovery strategy), each with named failure and description. Existing steps renumbered 2-6.</done>
</task>

<task type="auto">
  <name>Task 2: Add health output section and update next-action logic</name>
  <files>.claude/commands/research/progress/SKILL.md</files>
  <action>
Modify the Output section of the progress skill to add the infrastructure health display and update next-action behavior:

**Health section placement:** Add a new `### Infrastructure Health` section AFTER the project title/header line and BEFORE the existing `### Research Progress` phase table. Add a horizontal rule (`---`) after the health section to visually separate it.

**All-pass display:** When all 5 checks pass, output a single compact line:
```
### Infrastructure Health
Infrastructure: 5/5 checks passed
---
```

**Partial-failure display:** When any checks fail, output a summary line plus only the failures:
```
### Infrastructure Health
Infrastructure: 3/5 checks passed

- **PreToolUse Write hook missing** — this hook prevents unaudited writes to outputs/
- **settings.json invalid or missing** — the settings file must be valid JSON with hooks.PreToolUse and hooks.PreCompact arrays — without it, no audit gates are active

---
```

**Next action update:** Modify the existing `**Next action:**` line logic:
- If health failures exist, the next action line becomes: `**Next action:** Fix infrastructure issues above before continuing — [original next action from STATE.md]`
- If no health failures, next action remains unchanged from current behavior

**Guardrails addition:** Add a new guardrail (number 5) to the existing Guardrails section:
```
5. Report all infrastructure check results honestly. Do not skip checks or suppress failures to make the dashboard look clean.
```
  </action>
  <verify>
    <automated>grep -q "Infrastructure Health" .claude/commands/research/progress/SKILL.md && grep -q "5/5 checks passed" .claude/commands/research/progress/SKILL.md && grep -q "Fix infrastructure issues" .claude/commands/research/progress/SKILL.md && grep -q "Do not skip checks" .claude/commands/research/progress/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Progress output displays "Infrastructure Health" section after title, before phase table. All-pass shows compact single line. Failures show summary + failure list with names and descriptions. Next action prioritizes infrastructure fixes when failures exist. New guardrail prevents suppressing failures.</done>
</task>

</tasks>

<verification>
1. The progress skill file contains exactly 5 named health checks in the Process section
2. The Output section shows the Infrastructure Health header before the Research Progress table
3. All-pass case renders as a single compact line
4. Failure case renders with summary line + failure list (passing checks not enumerated)
5. Next action line includes infrastructure fix nudge when failures exist
6. Skill remains read-only with allowed-tools: Read, Grep, Glob
7. No new files created — all changes in progress/SKILL.md
</verification>

<success_criteria>
- INFRA-01 satisfied: infrastructure health status visible at top of progress output before project status
- Named failures: each of the 5 checks has a name and a WHY description
- Advisory pattern: health failures warn but do not block progress display
- Compact all-pass: healthy system adds minimal noise to output
</success_criteria>

<output>
After completion, create `.planning/phases/10-system-health-visibility/10-01-SUMMARY.md`
</output>
