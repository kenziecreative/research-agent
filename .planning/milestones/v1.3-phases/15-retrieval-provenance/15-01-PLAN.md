---
phase: 15-retrieval-provenance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/research/discover/SKILL.md
autonomous: true
requirements: [PROV-01, PROV-02]

must_haves:
  truths:
    - "After running discover, a retrieval log entry exists for each channel-tool execution containing query string, channel name, tool name, and all returned URLs"
    - "Log entries have typed fields (timestamp, phase, channel, tool, query, template, results_count, urls, status, degraded_to, deduped_count) that support filtering by phase, channel, or query without parsing free text"
    - "If retrieval-log.json does not exist or fails to parse, the discover run completes normally and the candidates file is unaffected"
    - "Multi-tool channels (web-search with Tavily+Exa) produce one log entry per tool execution, not one per channel"
  artifacts:
    - path: ".claude/commands/research/discover/SKILL.md"
      provides: "Log entry accumulation in Step 2 and log write step (Step 6a) after candidates file write"
      contains: "Step 6a: Append retrieval log entries"
  key_links:
    - from: "discover/SKILL.md Step 2g"
      to: "discover/SKILL.md Step 6a"
      via: "accumulated log entry objects passed from channel loop to batch write"
      pattern: "retrieval-log\\.json"
    - from: "discover/SKILL.md Step 6a"
      to: "research/reference/retrieval-log.json"
      via: "read-modify-write append of entry objects"
      pattern: "retrieval-log\\.json"
---

<objective>
Add retrieval provenance logging to the discover skill so every channel-tool execution is recorded with its exact query, channel, tool, returned URLs, and status.

Purpose: Makes discovery runs reproducible and auditable — a user can see exactly what was queried, through which channel, and what came back. Satisfies PROV-01 (log query/channel/URLs) and PROV-02 (structured queryable log).

Output: Modified discover/SKILL.md with log entry accumulation in Step 2 and a new Step 6a that batch-writes entries to retrieval-log.json after the candidates file write.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-retrieval-provenance/15-CONTEXT.md
@.planning/phases/15-retrieval-provenance/15-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From discover/SKILL.md Step 2g (lines 103-105) — the existing status print that log entries map from:
```markdown
**g.** Print result status: "{Channel Name}: found {N}" or "{Channel Name}: error — {reason}" or "{Channel Name}: degraded — using {fallback tool} fallback, found {N}" or "{Channel Name}: skipped (not mapped for this phase)"
```

From discover/SKILL.md Step 6 (line 138-140) — the insertion point for Step 6a:
```markdown
### Step 6: Write candidates file

Write to `research/discovery/{phase}-candidates.md`. Create the `research/discovery/` directory if it does not exist.
```

From discover/SKILL.md Step 7 (lines 212-214) — the verify step that comes after the new Step 6a:
```markdown
### Step 7: Print completion summary and offer to process

**Before printing the summary, verify the write succeeded.**
```

Entry schema per D-03:
```json
{
  "timestamp": "2026-04-20T14:23:00Z",
  "phase": "Phase 3: Financial Analysis",
  "channel": "web-search",
  "tool": "tavily",
  "query": "Acme Corp revenue 2024 site:sec.gov",
  "template": "B",
  "results_count": 6,
  "urls": ["https://example.com/article1", "https://example.com/article2"],
  "status": "found",
  "degraded_to": null,
  "deduped_count": 2
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add log entry accumulation to Step 2 channel execution loop</name>
  <files>.claude/commands/research/discover/SKILL.md</files>
  <read_first>
    - .claude/commands/research/discover/SKILL.md (the file being modified — read in full to see current state)
    - .planning/phases/15-retrieval-provenance/15-PATTERNS.md (Modification 1 section — exact pattern and insertion point for Step 2 accumulation)
    - .planning/phases/15-retrieval-provenance/15-CONTEXT.md (D-03 entry schema, D-04 write timing)
  </read_first>
  <action>
Insert a new sub-step after Step 2 sub-step **h** (the "If a channel fails..." bullet at current line 105). Add sub-step **i** that instructs the agent to accumulate a log entry object for each channel-tool execution.

The new sub-step **i** text to insert immediately after sub-step **h**:

```markdown

**i.** Accumulate a retrieval log entry for this channel-tool execution. Do not write to disk yet — collect entries in memory for batch write after Step 6. Each entry is a JSON object with these fields:

| Field | Type | Value |
|-------|------|-------|
| `timestamp` | string (ISO 8601) | Current timestamp at the moment of this channel execution |
| `phase` | string | Active phase name from STATE.md (Step 1 pre-check) |
| `channel` | string | Channel name from the Discovery Group (e.g., "web-search", "academic", "financial") |
| `tool` | string | Specific tool that executed (e.g., "tavily", "exa", "openalex", "crossref", "firecrawl", "websearch") |
| `query` | string | Exact query string or URL submitted to the tool (from Step 2d substitution) |
| `template` | string | Template letter used (A, B, or C from Step 2c selection) |
| `results_count` | integer | Count of URLs returned before dedup (from Step 2f) |
| `urls` | array of strings | All returned URLs from this call (pre-dedup) |
| `status` | string | One of: "found", "error", "degraded", "skipped" — mapped from Step 2g output |
| `degraded_to` | string or null | Fallback tool name if status is "degraded"; null otherwise |
| `deduped_count` | integer | Set to 0 here; updated after Step 4 dedup for multi-tool channels |

**Multi-tool channels:** When a channel runs multiple tools (e.g., web-search runs Tavily then Exa), accumulate one entry per tool execution. A web-search channel with both tools succeeding produces two entries: one with `tool: "tavily"`, one with `tool: "exa"`. The Exa entry's `deduped_count` is updated after Step 4 to reflect URL matches collapsed against Tavily results.

**Failed channels:** Accumulate an entry even when status is "error" or "skipped". Set `results_count: 0`, `urls: []`, and populate `status` accordingly. This provides a full audit trail of what was attempted, not just what succeeded.
```

Also add a sentence at the end of the existing Step 4 (Deduplicate) section, after the "Print: Deduplication..." line (current line 126). Insert:

```markdown

After deduplication, update the `deduped_count` field on the relevant log entry objects. For cross-tool dedup within a channel (e.g., Exa URLs that matched Tavily URLs), set `deduped_count` on the later tool's entry to the number of URLs removed. For dedup against an existing candidates file (re-run), set `deduped_count` on each affected entry to the number of that entry's URLs that were already present.
```
  </action>
  <verify>
    <automated>grep -c "Accumulate a retrieval log entry" .claude/commands/research/discover/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - discover/SKILL.md contains sub-step `**i.**` after sub-step `**h.**` in Step 2
    - Sub-step i contains the 11-field table with columns Field, Type, Value
    - Sub-step i contains "Multi-tool channels:" paragraph mentioning Tavily and Exa producing two entries
    - Sub-step i contains "Failed channels:" paragraph specifying entries for error/skipped status
    - Step 4 (Deduplicate) section contains "update the `deduped_count` field on the relevant log entry objects"
    - `grep -c "retrieval log entry" .claude/commands/research/discover/SKILL.md` returns at least 2
    - `grep "deduped_count" .claude/commands/research/discover/SKILL.md` returns matches in both Step 2 and Step 4 sections
  </acceptance_criteria>
  <done>Step 2 instructs agents to accumulate one log entry object per channel-tool execution with all 11 typed fields from D-03, including multi-tool and failed-channel handling. Step 4 instructs agents to update deduped_count after deduplication.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 6a — batch write retrieval log entries after candidates file</name>
  <files>.claude/commands/research/discover/SKILL.md</files>
  <read_first>
    - .claude/commands/research/discover/SKILL.md (re-read after Task 1 modifications to see current state)
    - .planning/phases/15-retrieval-provenance/15-PATTERNS.md (Modification 2 section — exact Step 6a text and error handling pattern)
    - .planning/phases/15-retrieval-provenance/15-CONTEXT.md (D-04 write timing, D-05 failure contract)
  </read_first>
  <action>
Insert a new section between Step 6 (Write candidates file) and Step 7 (Print completion summary). The new section is "Step 6a: Append retrieval log entries". Also renumber Step 7 to Step 7 (no renumbering needed — Step 6a is a sub-step, not a new numbered step, so Step 7 stays Step 7).

Insert the following markdown block immediately before the `### Step 7:` heading:

```markdown

### Step 6a: Append retrieval log entries

After the candidates file write and verification succeed, write the accumulated log entries to `research/reference/retrieval-log.json`.

**Read-modify-write pattern** (mirrors claim-graph.json write in audit-claims step 8b):

1. **Read** `research/reference/retrieval-log.json`.
   - If the file does not exist: initialize with `{"entries": []}` and proceed.
   - If it exists but fails to parse as JSON: log a warning and skip the log write without failing the discovery run. Print: "WARNING: retrieval-log.json could not be parsed — log entries not written. Discovery output is unaffected." Do not block the user from proceeding.
   - If it parses correctly: load the existing `entries` array.
2. **Append** all log entry objects accumulated during Step 2 to the `entries` array.
   - Write the entire updated JSON back to `research/reference/retrieval-log.json`.
   - One entry per channel-tool execution. Entry order within this batch: channel priority order (Primary channels before Secondary channels), matching the execution order from Step 2.
3. **Verify** the write succeeded: re-read the file and confirm it parses as valid JSON and the entry count has increased by the expected number. If verification fails: print "WARNING: retrieval-log.json write failed — log entries not persisted. Discovery output is unaffected." Do not fail or retry discovery.

**Failure contract:** If the discover run crashes before Step 6 (the candidates file write), no log entries are written. If Step 6 fails (candidates file not written), do not attempt Step 6a. The candidates file and retrieval log are peers — both exist or neither exists for a given run.

```

Also add one entry to the Guardrails section (after guardrail 8, current line 453). Insert guardrail 9:

```markdown
9. **Retrieval log write must never block discovery.** If `retrieval-log.json` cannot be read, parsed, or written, print a WARNING and proceed. The candidates file is the primary artifact — the retrieval log is supplementary infrastructure. Never abort discovery, never retry the write, never ask the user to fix the file before continuing.
```

Also add one entry to the Common Failure Modes table (before the closing `---` of that section). Insert a new row:

```markdown
| Retrieval log write failure blocking discovery output | Step 6a non-blocking contract: if retrieval-log.json cannot be read, parsed, or written, print WARNING and proceed. The candidates file is the primary artifact. Never abort, retry, or ask the user to resolve before continuing. |
```
  </action>
  <verify>
    <automated>grep -c "Step 6a" .claude/commands/research/discover/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - discover/SKILL.md contains heading `### Step 6a: Append retrieval log entries`
    - Step 6a appears between Step 6 (Write candidates file) and Step 7 (Print completion summary)
    - Step 6a contains 3 numbered sub-steps: Read, Append, Verify
    - Step 6a contains "WARNING: retrieval-log.json could not be parsed" string
    - Step 6a contains "WARNING: retrieval-log.json write failed" string
    - Step 6a contains "Failure contract:" paragraph mentioning candidates file and retrieval log as peers
    - Guardrails section contains guardrail 9 with text "Retrieval log write must never block discovery"
    - Common Failure Modes table contains row with "Retrieval log write failure blocking discovery output"
    - `grep -c "retrieval-log.json" .claude/commands/research/discover/SKILL.md` returns at least 8
  </acceptance_criteria>
  <done>Step 6a batch-writes all accumulated log entries to retrieval-log.json using the read-modify-write pattern with non-blocking error handling. Guardrail 9 and a failure mode entry reinforce the non-blocking contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> retrieval-log.json | Agent writes accumulated log data to a JSON file in the project directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | retrieval-log.json | accept | File is local project data with no auth boundary; user owns the file system. Advisory-only data — tampering has no gate consequence. |
| T-15-02 | Denial of Service | Step 6a write failure | mitigate | Non-blocking write contract: parse failure or write failure prints WARNING and continues. Discovery output (candidates file) is never affected by log write failures. |
| T-15-03 | Information Disclosure | URLs logged in retrieval-log.json | accept | URLs are already visible in the candidates file — the log adds no new exposure surface. File is local-only. |
</threat_model>

<verification>
1. `grep "Step 6a" .claude/commands/research/discover/SKILL.md` returns heading match
2. `grep "retrieval-log.json" .claude/commands/research/discover/SKILL.md` returns 8+ matches across Step 2i, Step 4, Step 6a, Guardrail 9, and Failure Modes table
3. `grep "Accumulate a retrieval log entry" .claude/commands/research/discover/SKILL.md` returns 1 match in Step 2
4. `grep "deduped_count" .claude/commands/research/discover/SKILL.md` returns matches in Step 2 and Step 4
5. Step 6a appears after Step 6 heading and before Step 7 heading (visual line order check)
</verification>

<success_criteria>
- discover/SKILL.md contains Step 2 sub-step i with 11-field entry schema table and multi-tool/failed-channel instructions
- discover/SKILL.md contains Step 4 deduped_count update instruction
- discover/SKILL.md contains Step 6a with read-modify-write pattern, non-blocking error handling, and failure contract
- discover/SKILL.md contains Guardrail 9 (log write non-blocking)
- discover/SKILL.md contains failure mode row for log write blocking discovery
- No existing Step 2 sub-steps (a-h), Step 6, or Step 7 content is removed or altered in meaning
</success_criteria>

<output>
After completion, create `.planning/phases/15-retrieval-provenance/15-01-SUMMARY.md`
</output>
