---
phase: 15-retrieval-provenance
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/research/init/SKILL.md
autonomous: true
requirements: [PROV-02]

must_haves:
  truths:
    - "Running /research:init scaffolds an empty retrieval-log.json alongside canonical-figures.json and claim-graph.json"
    - "The init verify step checks that retrieval-log.json exists after scaffolding"
    - "The CLAUDE.md directory tree generated by init lists retrieval-log.json under reference/"
  artifacts:
    - path: ".claude/commands/research/init/SKILL.md"
      provides: "Scaffolding write for retrieval-log.json, verify checklist entry, and CLAUDE.md directory tree entry"
      contains: "retrieval-log.json"
  key_links:
    - from: "init/SKILL.md Step 5"
      to: "research/reference/retrieval-log.json"
      via: "Write statement creating empty file with {\"entries\": []}"
      pattern: "retrieval-log\\.json"
    - from: "init/SKILL.md Step 6"
      to: "research/reference/retrieval-log.json"
      via: "ls verification checklist entry"
      pattern: "reference/retrieval-log\\.json"
---

<objective>
Add retrieval-log.json scaffolding to the init skill so new research projects start with an empty retrieval log file ready for discover to populate.

Purpose: Ensures the retrieval log exists from project creation, matching the canonical-figures.json and claim-graph.json scaffolding pattern. Supports PROV-02 (structured queryable log exists as a first-class project artifact).

Output: Modified init/SKILL.md with retrieval-log.json write in Step 5, verify checklist entry in Step 6, and directory tree entry in the CLAUDE.md template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-retrieval-provenance/15-CONTEXT.md
@.planning/phases/15-retrieval-provenance/15-PATTERNS.md
@.planning/phases/11-claim-graph-foundation/11-02-SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From init/SKILL.md line 686 — the claim-graph.json write to mirror:
```markdown
- Write `research/reference/claim-graph.json` with initial content `{"claims": []}` — this is the claim graph registry, populated by `/research:audit-claims` during each phase's Verify step.
```

From init/SKILL.md lines 409-410 — the current reference/ directory tree:
```markdown
│   ├── canonical-figures.json # Single source of truth for cross-phase numbers
│   └── claim-graph.json       # Claim graph registry, written by /research:audit-claims
```

From init/SKILL.md lines 731-732 — the current verify checklist entries:
```markdown
   - `reference/canonical-figures.json`
   - `reference/claim-graph.json`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add retrieval-log.json scaffolding to init SKILL.md (Step 5 write, Step 6 verify, CLAUDE.md tree)</name>
  <files>.claude/commands/research/init/SKILL.md</files>
  <read_first>
    - .claude/commands/research/init/SKILL.md (the file being modified — read in full to see current state, especially lines 686, 409-410, and 731-732)
    - .planning/phases/15-retrieval-provenance/15-PATTERNS.md (init/SKILL.md section — exact insertion points and text for all three modifications)
    - .planning/phases/11-claim-graph-foundation/11-02-SUMMARY.md (how claim-graph.json was added — same pattern to follow)
  </read_first>
  <action>
Three insertions in init/SKILL.md, each mirroring the claim-graph.json pattern established in Phase 11:

**Insertion 1 — Step 5 "Other Files" block (after line 686):**

Insert one bullet immediately after the claim-graph.json write statement (line 686: `- Write \`research/reference/claim-graph.json\` with initial content \`{"claims": []}\``). The new bullet:

```markdown
- Write `research/reference/retrieval-log.json` with initial content `{"entries": []}` — this is the retrieval log registry, populated by `/research:discover` after each discovery run.
```

**Insertion 2 — CLAUDE.md directory tree (lines 409-410):**

Change line 410 from:
```
│   └── claim-graph.json       # Claim graph registry, written by /research:audit-claims
```
To:
```
│   ├── claim-graph.json       # Claim graph registry, written by /research:audit-claims
│   └── retrieval-log.json     # Retrieval log registry, written by /research:discover
```

Note: `claim-graph.json` changes from `└──` (last item) to `├──` (non-last item) because `retrieval-log.json` is now the last item in the `reference/` directory listing.

**Insertion 3 — Step 6 verify checklist (after line 732):**

Insert one line immediately after `- \`reference/claim-graph.json\`` (line 732):

```
   - `reference/retrieval-log.json`
```
  </action>
  <verify>
    <automated>grep -c "retrieval-log.json" .claude/commands/research/init/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "retrieval-log.json" .claude/commands/research/init/SKILL.md` returns exactly 3
    - init/SKILL.md contains `Write \`research/reference/retrieval-log.json\` with initial content \`{"entries": []}\``
    - init/SKILL.md directory tree has `claim-graph.json` with `├──` (not `└──`)
    - init/SKILL.md directory tree has `retrieval-log.json` with `└──` as the last reference/ entry
    - init/SKILL.md Step 6 verify checklist contains `reference/retrieval-log.json` on the line after `reference/claim-graph.json`
    - The retrieval-log.json tree comment reads `# Retrieval log registry, written by /research:discover`
    - The Step 5 write statement says `populated by \`/research:discover\` after each discovery run`
    - No existing content has been removed — canonical-figures.json and claim-graph.json entries remain unchanged
  </acceptance_criteria>
  <done>init/SKILL.md scaffolds retrieval-log.json with empty {"entries": []} content in Step 5, verifies its existence in Step 6, and lists it in the CLAUDE.md directory tree template — matching the exact pattern used for claim-graph.json in Phase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> retrieval-log.json | Agent writes an empty JSON file during project scaffolding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | Empty retrieval-log.json at init | accept | Trivial empty-struct write at init time; no data to tamper with. Same risk profile as claim-graph.json scaffolding (T-11-03 from Phase 11). |
</threat_model>

<verification>
1. `grep -c "retrieval-log.json" .claude/commands/research/init/SKILL.md` returns 3
2. `grep "├── claim-graph.json" .claude/commands/research/init/SKILL.md` returns match (no longer └──)
3. `grep "└── retrieval-log.json" .claude/commands/research/init/SKILL.md` returns match (new last item)
4. `grep "reference/retrieval-log.json" .claude/commands/research/init/SKILL.md` returns match in verify checklist
</verification>

<success_criteria>
- init/SKILL.md Step 5 writes retrieval-log.json with {"entries": []} content
- init/SKILL.md Step 6 verify checklist includes retrieval-log.json
- init/SKILL.md CLAUDE.md directory tree lists retrieval-log.json as last reference/ entry
- claim-graph.json tree connector changed from └── to ├──
- No existing init content removed or altered in meaning
</success_criteria>

<output>
After completion, create `.planning/phases/15-retrieval-provenance/15-02-SUMMARY.md`
</output>
