---
phase: 14-web-channel-diversity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/reference/discovery/channel-playbooks/web-search.md
autonomous: true
requirements:
  - DISC-03
  - DISC-04

must_haves:
  truths:
    - "Web-search playbook documents Exa as API 2 alongside Tavily"
    - "Exa query templates exist with the same structure as Tavily templates"
    - "Exa has its own degradation path independent of Tavily's 3-tier chain"
    - "Section 8 dedup rules define URL-based deduplication with Tavily priority"
    - "Inline source attribution tags [Tavily], [Exa], [Tavily+Exa] are documented"
    - "Combined status line format is specified"
  artifacts:
    - path: ".claude/reference/discovery/channel-playbooks/web-search.md"
      provides: "Full Exa integration: tool config, query templates, degradation, rate limits, Section 8 dedup and attribution"
      contains: "API 2 — Exa"
  key_links:
    - from: "web-search.md Section 8"
      to: "discover/SKILL.md Step 4"
      via: "URL dedup contract (exact match)"
      pattern: "Deduplication.*URL"
---

<objective>
Extend the web-search channel playbook with Exa neural search as API 2 alongside Tavily, including tool configuration, query templates, independent degradation path, rate limits, and a new Section 8 with deduplication rules and source attribution tags.

Purpose: Fulfills DISC-03 (Exa as parallel web-search tier) and DISC-04 (dedup Exa against Tavily) at the playbook documentation level.
Output: Updated web-search.md with complete Exa integration following the academic.md multi-API pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-web-channel-diversity/14-CONTEXT.md
@.planning/phases/14-web-channel-diversity/14-PATTERNS.md
@.planning/phases/13-academic-evidence-layer-expansion/13-01-SUMMARY.md

<interfaces>
<!-- Pattern source: academic.md multi-API structure -->
From .claude/reference/discovery/channel-playbooks/academic.md Section 2:
```markdown
**API 2 — Crossref:**
- Base URL: `https://api.crossref.org/works`
- Authentication: None required — polite pool via `mailto` query parameter
- Response format: JSON
- Returns: Paper metadata including DOI, title, authors, citation count
```

From academic.md Section 8:
```markdown
## 8. Deduplication and Priority Rules

**Query order:** OpenAlex first, Crossref second, Unpaywall third (inline during same pass).

**Deduplication by DOI:** If a paper appears in both OpenAlex and Crossref results (matched by DOI string equality after normalization to lowercase), the OpenAlex entry is kept.

**Channel status line:**
Academic: {N} results (OpenAlex: {n1}, Crossref-only: {n2}, Unpaywall upgrades: {n3}) [degraded: {list}]
```

From web-search.md existing structure:
- Section 1: Channel Overview
- Section 2: Tool Configuration (Tavily only)
- Section 3: Query Templates (A, B, C — Tavily only)
- Section 4: Credibility Tiers
- Section 5: Source Status Taxonomy
- Section 6: Degradation Behavior (Tavily 3-tier chain)
- Section 7: Rate Limits (Tavily, Firecrawl, WebSearch)
- (No Section 8 currently exists)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Exa tool configuration, query templates, degradation, and rate limits to web-search.md</name>
  <files>.claude/reference/discovery/channel-playbooks/web-search.md</files>
  <read_first>
    - .claude/reference/discovery/channel-playbooks/web-search.md
    - .claude/reference/discovery/channel-playbooks/academic.md
    - .planning/phases/14-web-channel-diversity/14-PATTERNS.md
  </read_first>
  <action>
Extend web-search.md with Exa as API 2. Make the following additions to existing sections:

**Section 1 (Channel Overview):** Update the description line to mention both Tavily and Exa. Change "General web discovery via Tavily search" to "General web discovery via Tavily search and Exa neural search". Update the frontmatter `description` field similarly.

**Section 2 (Tool Configuration):** After the existing Tavily configuration block, add:

```markdown
**API 2 — Exa (Secondary, Neural Search):**

**Tool:** Exa Search API (via Bash curl)

**Base URL:** `https://api.exa.ai/search`

**Authentication:** `EXA_API_KEY` environment variable. Free tier: 1,000 searches/month.

**Core parameters:**

| Parameter | Value | Notes |
|-----------|-------|-------|
| `query` | (string) | Natural language query; Exa optimizes for semantic relevance |
| `numResults` | 8 | Per D-03: 8-result cap per tool within channel |
| `type` | `auto` | Per D-04: routes to neural or keyword via Exa's internal categorization |
| `useAutoprompt` | `true` | Let Exa refine the query for better semantic retrieval |
| `startPublishedDate` | (ISO date, optional) | Filter to results published after this date |
| `category` | (optional) | One of: company, research paper, news, pdf, github, tweet, personal site |

**Response format:** JSON with `results[]` array. Each result has: `title`, `url`, `publishedDate`, `author`, `score`.
```

**Section 3 (Query Templates):** After existing Templates A/B/C, add:

```markdown
### Exa Query Templates

### Template D — Exa Topic Search (Neural)

```bash
curl -s -X POST "https://api.exa.ai/search" \
  -H "x-api-key: $EXA_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "{topic} {context_keywords}",
    "numResults": 8,
    "type": "auto",
    "useAutoprompt": true
  }'
```

**Placeholder substitution:**
- `{topic}` — the primary research subject
- `{context_keywords}` — 1-3 constraining terms from the research brief

**Use when:** Broad topic coverage is needed and Tavily has already returned its results. Exa's neural mode surfaces semantically relevant pages that SEO-optimized results may miss.

---

### Template E — Exa Entity Search

```bash
curl -s -X POST "https://api.exa.ai/search" \
  -H "x-api-key: $EXA_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "{entity_name} {entity_type}",
    "numResults": 8,
    "type": "auto",
    "useAutoprompt": true,
    "category": "{category}"
  }'
```

**Placeholder substitution:**
- `{entity_name}` — proper name of the entity
- `{entity_type}` — category term that disambiguates
- `{category}` — (optional) one of: company, research paper, news, personal site. Omit the field entirely if no category applies.

**Use when:** Researching a specific named entity. Use `category` to bias results toward the entity type.

---

### Template F — Exa Recent Search

```bash
curl -s -X POST "https://api.exa.ai/search" \
  -H "x-api-key: $EXA_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "{topic} recent developments",
    "numResults": 8,
    "type": "auto",
    "useAutoprompt": true,
    "startPublishedDate": "{iso_date}",
    "category": "news"
  }'
```

**Placeholder substitution:**
- `{topic}` — subject of interest
- `{iso_date}` — ISO 8601 date (e.g., "2024-01-01") for the start of the recency window

**Use when:** The research brief calls for recent developments. Use `startPublishedDate` to bound the time window.
```

**Section 6 (Degradation Behavior):** After the existing Tavily 3-tier degradation chain and its recovery note, add a new subsection:

```markdown
**Exa degradation (if api.exa.ai unavailable):**

If Exa is unavailable (API key absent, timeout > 15 seconds, HTTP 5xx, HTTP 429, or non-zero exit from curl), skip silently. Log a one-line channel status note: `Exa: unavailable — web search results from Tavily only`. Do not retry. Do not block. Do not trigger Tavily fallbacks — Exa and Tavily are independent parallel tools within the same channel (per D-13). Discovery continues with Tavily results only.

**Unavailability criteria for Exa:**
- `EXA_API_KEY` environment variable not set
- HTTP 5xx response from api.exa.ai
- Request timeout > 15 seconds
- HTTP 429 (rate limit exceeded)
- Non-zero exit code from curl command
```

**Section 7 (Rate Limits):** After the existing Tavily, Firecrawl, and WebSearch entries, add:

```markdown
**Exa (API 2, parallel neural search):**
- Free tier: 1,000 searches/month
- No per-second throttle required for typical research sessions (1-20 searches per session)
- Monthly cap is shared with all Exa usage — budget searches conservatively if approaching limit
- Rate limit detection: 429 response or non-zero exit from curl → trigger Exa degradation (Section 6)
```
  </action>
  <verify>
    <automated>grep -c "api.exa.ai" .claude/reference/discovery/channel-playbooks/web-search.md | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
    - web-search.md contains "API 2 — Exa" in Section 2
    - web-search.md contains "Template D — Exa Topic Search" in Section 3
    - web-search.md contains "Template E — Exa Entity Search" in Section 3
    - web-search.md contains "Template F — Exa Recent Search" in Section 3
    - web-search.md contains "Exa degradation (if api.exa.ai unavailable):" in Section 6
    - web-search.md contains "EXA_API_KEY" at least twice (Section 2 config and Section 6 degradation)
    - web-search.md contains "1,000 searches/month" in Section 7 Exa entry
    - web-search.md frontmatter description references Exa
    - Existing Tavily Templates A/B/C remain unchanged
    - Existing Tavily degradation 3-tier chain remains unchanged
  </acceptance_criteria>
  <done>web-search.md Sections 1-3, 6, and 7 are extended with complete Exa configuration, three query templates (D/E/F), independent degradation path, and rate limits. Existing Tavily content is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add Section 8 — Deduplication, Priority Rules, and Source Attribution to web-search.md</name>
  <files>.claude/reference/discovery/channel-playbooks/web-search.md</files>
  <read_first>
    - .claude/reference/discovery/channel-playbooks/web-search.md
    - .claude/reference/discovery/channel-playbooks/academic.md
    - .planning/phases/14-web-channel-diversity/14-CONTEXT.md
  </read_first>
  <action>
Add a new Section 8 at the end of web-search.md (after Section 7). This section follows the academic.md Section 8 pattern exactly, adapted for URL-based dedup per D-07 through D-11.

Add the following content:

```markdown
---

## 8. Deduplication and Priority Rules

**Query order:** Tavily first, Exa second (per D-02). Tavily executes and returns up to 8 results. Exa then executes and returns up to 8 results. Dedup collapses overlaps before presenting combined candidates.

**Deduplication by URL:** If a URL appears in both Tavily and Exa results (matched by exact URL string equality — no normalization, no trailing-slash stripping, no protocol comparison), the Tavily entry is kept as the primary candidate. The Exa entry is silently dropped. The surviving candidate receives the `[Tavily+Exa]` attribution tag to indicate both engines found it.

**No data merge:** Unlike academic.md's Crossref gap-filling, web-search dedup is pure collapse. Tavily metadata (title, snippet) is kept; Exa metadata for the duplicate is discarded. This keeps the contract simple — URL string equality is the only dedup key.

**Source attribution tags:** Every candidate in the web-search channel carries an engine attribution tag after its status tag:

| Tag | Meaning |
|-----|---------|
| `[Tavily]` | Found by Tavily only |
| `[Exa]` | Found by Exa only (not in Tavily results) |
| `[Tavily+Exa]` | Found by both; Tavily entry kept, Exa match collapsed |

**Candidate format with attribution:**
```
- [ACCESSIBLE] [Tavily] Title — URL
- [DISCOVERED] [Exa] Title — URL
- [ACCESSIBLE] [Tavily+Exa] Title — URL
```

The engine tag appears immediately after the status tag (`[ACCESSIBLE]`, `[DISCOVERED]`, `[PROCESSED]`) and before the title. This extends the existing tag idiom used by `[Firecrawl fallback]` and `[WebSearch fallback]`.

**Channel status line:** After both APIs complete (or Exa degrades), report a single web-search channel status line:

```
Web Search: {N} results (Tavily: {n1}, Exa-only: {n2}, deduped: {n3}) [degraded: {list}]
```

Where:
- `{N}` = total unique candidates presented (n1 + n2, after dedup)
- `{n1}` = results from Tavily (including those that were also in Exa, tagged [Tavily+Exa])
- `{n2}` = results from Exa that were NOT in Tavily results
- `{n3}` = number of Exa results dropped due to URL match with Tavily
- `{list}` = names any tool that was unavailable during this run (e.g., "Exa"); empty if both succeeded

**Exa-only mode:** If Tavily degrades but Exa succeeds, all Exa results are tagged `[Exa]` and reported normally. The status line shows `Tavily: 0` and notes `[degraded: Tavily]`.

**Tavily-only mode:** If Exa degrades, all Tavily results are tagged `[Tavily]`. Status line shows `Exa-only: 0, deduped: 0` and notes `[degraded: Exa]`.
```
  </action>
  <verify>
    <automated>grep -c "Deduplication and Priority" .claude/reference/discovery/channel-playbooks/web-search.md | xargs test 1 -eq</automated>
  </verify>
  <acceptance_criteria>
    - web-search.md contains "## 8. Deduplication and Priority Rules" heading
    - web-search.md contains "Deduplication by URL:" paragraph
    - web-search.md contains "[Tavily]" tag definition in the attribution table
    - web-search.md contains "[Exa]" tag definition in the attribution table
    - web-search.md contains "[Tavily+Exa]" tag definition in the attribution table
    - web-search.md contains the status line format: "Web Search: {N} results (Tavily: {n1}, Exa-only: {n2}, deduped: {n3})"
    - web-search.md contains "Query order:" specifying "Tavily first, Exa second"
    - web-search.md contains "Exa-only mode:" degradation scenario
    - web-search.md contains "Tavily-only mode:" degradation scenario
  </acceptance_criteria>
  <done>web-search.md has a complete Section 8 defining URL-based deduplication, Tavily priority, source attribution tags ([Tavily], [Exa], [Tavily+Exa]), candidate format examples, combined status line format, and both degradation scenarios (Exa-only mode, Tavily-only mode).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Exa API | Outbound API call with user's API key; response is untrusted JSON |
| Exa results -> Candidate file | URLs from Exa rendered in discovery output; user reviews before processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | EXA_API_KEY in curl commands | accept | Key is in env var (not hardcoded); curl examples use $EXA_API_KEY shell expansion; same pattern as TAVILY_API_KEY |
| T-14-02 | Spoofing | Exa API response URLs | accept | URLs are presented for user review before any processing (DISCOVERED status); no auto-fetch of returned URLs without user action |
| T-14-03 | Denial of Service | Exa rate limit exhaustion | mitigate | Playbook documents 1,000/month cap; degradation path skips silently on 429; discovery continues with Tavily only |
| T-14-04 | Tampering | Malicious URL in Exa results | accept | Same risk as Tavily results; existing source status workflow (DISCOVERED -> ACCESSIBLE -> PROCESSED) gates user verification before any content extraction |
</threat_model>

<verification>
After plan completion:
1. `grep "API 2 — Exa" .claude/reference/discovery/channel-playbooks/web-search.md` returns a match
2. `grep -c "Template [DEF]" .claude/reference/discovery/channel-playbooks/web-search.md` returns 3
3. `grep "Deduplication and Priority" .claude/reference/discovery/channel-playbooks/web-search.md` returns a match
4. `grep "\[Tavily+Exa\]" .claude/reference/discovery/channel-playbooks/web-search.md` returns a match
5. `grep "Exa degradation" .claude/reference/discovery/channel-playbooks/web-search.md` returns a match
6. Existing Templates A, B, C still present: `grep -c "Template [ABC]" web-search.md` returns 3
</verification>

<success_criteria>
web-search.md is a complete multi-API channel playbook (Tavily + Exa) with tool configuration, query templates (A-F), credibility tiers, source status taxonomy, independent degradation paths for each tool, rate limits for all tools, and Section 8 deduplication/attribution rules — following the established academic.md pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/14-web-channel-diversity/14-01-SUMMARY.md`
</output>
