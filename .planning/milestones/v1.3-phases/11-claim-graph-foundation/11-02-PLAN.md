---
phase: 11-claim-graph-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/research/init/SKILL.md
  - .claude/agents/research-integrity.md
autonomous: true
requirements:
  - TRACE-01

must_haves:
  truths:
    - "Running /research:init creates an empty claim-graph.json alongside canonical-figures.json"
    - "Init verification step confirms claim-graph.json exists"
    - "Research-integrity agent checks claim graph consistency when reviewing phase outputs"
    - "The CLAUDE.md directory tree generated by init includes claim-graph.json"
  artifacts:
    - path: ".claude/commands/research/init/SKILL.md"
      provides: "claim-graph.json scaffolding in Step 5 Other Files, verification in Step 6, directory tree update"
      contains: "claim-graph.json"
    - path: ".claude/agents/research-integrity.md"
      provides: "Check 9 — Claim Graph Consistency"
      contains: "Claim Graph Consistency"
  key_links:
    - from: ".claude/commands/research/init/SKILL.md"
      to: "research/reference/claim-graph.json"
      via: "Step 5 Other Files scaffolding write"
      pattern: "claim-graph\\.json.*claims"
    - from: ".claude/agents/research-integrity.md"
      to: "research/reference/claim-graph.json"
      via: "Check 9 reads claim-graph.json and compares against audit reports"
      pattern: "claim-graph\\.json"
---

<objective>
Extend init SKILL.md to scaffold an empty claim-graph.json at project creation, and extend research-integrity.md with check 9 for claim graph consistency.

Purpose: The graph file must exist before audit-claims can write to it (init creates it), and the integrity agent must be able to detect drift between the graph and audit reports (check 9 enables this).

Output: Modified init/SKILL.md with scaffolding + verification + directory tree updates; modified research-integrity.md with check 9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-claim-graph-foundation/11-CONTEXT.md
@.planning/phases/11-claim-graph-foundation/11-PATTERNS.md

<interfaces>
<!-- Init SKILL.md insertion points -->

From .claude/commands/research/init/SKILL.md:
- Line 683: `- Copy `.claude/reference/templates/canonical-figures.json` to `research/reference/canonical-figures.json``
  (new claim-graph.json write goes immediately after this line)
- Lines 399-413: Directory tree in CLAUDE.md template, contains:
  ```
  │   └── canonical-figures.json # Single source of truth for cross-phase numbers
  ```
  (add claim-graph.json line after this)
- Lines 715-730: Step 6 verification checklist, contains:
  ```
  - `reference/canonical-figures.json`
  ```
  (add `reference/claim-graph.json` to this list)

<!-- Research-integrity.md insertion points -->

From .claude/agents/research-integrity.md:
- Check 5 (lines 40-43): Cross-Phase Drift — pattern to mirror for check 9
- Check 8 (lines 54-61): Source Material Coverage — last numbered check, check 9 goes after
- Lines 63-73: "How to Use This Agent" section — add claim-graph.json trigger
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add claim-graph.json scaffolding and verification to init SKILL.md</name>
  <files>.claude/commands/research/init/SKILL.md</files>
  <read_first>
    - .claude/commands/research/init/SKILL.md (full file — understand Step 5 Other Files section around line 683, directory tree around line 409, Step 6 verification around line 728)
    - .planning/phases/11-claim-graph-foundation/11-PATTERNS.md (lines 93-117 — exact init modification pattern)
  </read_first>
  <action>
Three insertions into init/SKILL.md:

**Insertion 1 — Step 5 Other Files (after the canonical-figures.json copy line):**

Find the line:
```
- Copy `.claude/reference/templates/canonical-figures.json` to `research/reference/canonical-figures.json`
```

Insert immediately after it:
```
- Write `research/reference/claim-graph.json` with initial content `{"claims": []}` — this is the claim graph registry, populated by `/research:audit-claims` during each phase's Verify step.
```

**Insertion 2 — Directory tree in CLAUDE.md template (after canonical-figures.json line):**

Find the line in the directory tree that reads:
```
│   └── canonical-figures.json # Single source of truth for cross-phase numbers
```

Change `└──` to `├──` on that line (since it's no longer the last item in reference/), and insert a new line after it:
```
│   └── claim-graph.json       # Claim graph registry, written by /research:audit-claims
```

**Insertion 3 — Step 6 verification checklist:**

Find the checklist item:
```
   - `reference/canonical-figures.json`
```

Insert immediately after it:
```
   - `reference/claim-graph.json`
```
  </action>
  <verify>
    <automated>grep -n "claim-graph.json" .claude/commands/research/init/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - init/SKILL.md contains the string `Write `research/reference/claim-graph.json` with initial content `{"claims": []}`
    - init/SKILL.md contains `claim-graph.json` in the directory tree section (within lines 395-420)
    - init/SKILL.md contains `reference/claim-graph.json` in the Step 6 verification checklist (within lines 710-735)
    - The directory tree shows canonical-figures.json with `├──` (not `└──`) and claim-graph.json with `└──` as the last item
    - `grep -c "claim-graph.json" .claude/commands/research/init/SKILL.md` returns 3 (one per insertion)
  </acceptance_criteria>
  <done>init/SKILL.md scaffolds claim-graph.json with {"claims": []} content, includes it in the directory tree, and verifies its existence in Step 6</done>
</task>

<task type="auto">
  <name>Task 2: Add check 9 (Claim Graph Consistency) to research-integrity.md</name>
  <files>.claude/agents/research-integrity.md</files>
  <read_first>
    - .claude/agents/research-integrity.md (full file — understand check 5 structure at lines 40-43, check 8 ending, "How to Use" section at lines 63-73)
    - .planning/phases/11-claim-graph-foundation/11-PATTERNS.md (lines 119-147 — exact check 9 text and How to Use update)
  </read_first>
  <action>
Two insertions into research-integrity.md:

**Insertion 1 — New check 9 (after check 8, before "## How to Use This Agent"):**

Find the line `## How to Use This Agent` and insert the following block before it:

```markdown
### 9. Claim Graph Consistency
- This check applies only when `research/reference/claim-graph.json` exists and the file under review is a phase output (`research/outputs/`) or draft (`research/drafts/`).
- Read `research/reference/claim-graph.json`. If the file does not exist, skip this check without comment (the graph is scaffolded at init but populated only after the first audit-claims run).
- For each claim node in the graph whose `phase` matches the current phase under review: verify the `confidence_tier` in the graph matches the tier shown for that section in the audit report (if an audit report exists in `research/audits/`).
- Flag: "CLAIM GRAPH INCONSISTENCY: claim [id] in claim-graph.json has confidence_tier [A], but the audit report for [phase output] shows [B] for section [section]. The graph may have been written from a stale audit pass."
- This check is advisory — it surfaces drift between the graph and the audit record, but does not block promotion.

```

**Insertion 2 — "How to Use This Agent" section trigger list:**

Find the line that starts with `- Any time something feels like it might have drifted` (the last bullet in the invocation list) and insert a new bullet before it:

```
- After `/research:audit-claims` writes claim nodes to claim-graph.json (check 9 catches graph-audit consistency drift)
```
  </action>
  <verify>
    <automated>grep -n "Claim Graph Consistency" .claude/agents/research-integrity.md && grep -n "CLAIM GRAPH INCONSISTENCY" .claude/agents/research-integrity.md && grep -n "claim-graph.json" .claude/agents/research-integrity.md</automated>
  </verify>
  <acceptance_criteria>
    - research-integrity.md contains `### 9. Claim Graph Consistency` as a section heading
    - research-integrity.md contains the flag prefix `CLAIM GRAPH INCONSISTENCY:`
    - research-integrity.md contains `This check is advisory` (non-blocking, matching check 7 pattern)
    - research-integrity.md contains `skip this check without comment` (graceful absence handling)
    - research-integrity.md "How to Use" section contains `claim-graph.json` trigger bullet
    - Check 9 appears AFTER check 8 and BEFORE `## How to Use This Agent`
    - `grep -c "claim-graph.json" .claude/agents/research-integrity.md` returns at least 3
  </acceptance_criteria>
  <done>research-integrity.md has check 9 that reads claim-graph.json, compares confidence tiers against audit reports, flags inconsistencies with structured CLAIM GRAPH INCONSISTENCY prefix, and is listed as an invocation trigger</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init -> claim-graph.json | Agent creates an empty JSON file during project scaffolding |
| integrity-agent -> claim-graph.json | Agent reads the graph for advisory consistency checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | claim-graph.json at init | accept | Init writes a trivial empty structure (`{"claims": []}`). No sensitive data. Single-user tool with agent-only access. |
| T-11-04 | Information Disclosure | check 9 reads claim-graph.json | accept | Graph contains claim text extracted from user's own research drafts. No cross-user or cross-project data exposure. Read-only check. |
</threat_model>

<verification>
1. `grep -c "claim-graph.json" .claude/commands/research/init/SKILL.md` returns 3
2. `grep -c "claim-graph.json" .claude/agents/research-integrity.md` returns at least 3
3. Check 9 heading exists in research-integrity.md
4. Init verification checklist includes claim-graph.json
</verification>

<success_criteria>
Init scaffolds claim-graph.json with empty claims array and verifies its existence. Research-integrity agent has check 9 that compares claim graph confidence tiers against audit reports and flags drift as advisory (non-blocking).
</success_criteria>

<output>
After completion, create `.planning/phases/11-claim-graph-foundation/11-02-SUMMARY.md`
</output>
