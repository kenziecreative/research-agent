---
phase: 11-claim-graph-foundation
verified: 2026-04-20T00:00:00Z
status: human_needed
score: 8/8
overrides_applied: 0
human_verification:
  - test: "Run /research:init on a new project and confirm research/reference/claim-graph.json is created with {\"claims\": []} content"
    expected: "File exists at research/reference/claim-graph.json with valid JSON and an empty claims array"
    why_human: "Init SKILL.md has the correct write instruction but only runtime execution confirms the agent follows it"
  - test: "Run /research:audit-claims on a draft with known factual claims and inspect research/reference/claim-graph.json after"
    expected: "Claim nodes exist with all 9 schema fields (id, text, phase, section, confidence_tier, source_files, figure_ids, evidence_directness, source_count); re-auditing the same draft replaces nodes rather than duplicating them"
    why_human: "Step 8b instructions are complete but actual agent execution of the write path cannot be verified without running an audit"
---

# Phase 11: Claim Graph Foundation — Verification Report

**Phase Goal:** Claims are represented as discrete graph nodes with typed edges to their source notes and any canonical figures they reference
**Verified:** 2026-04-20
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | audit-claims writes a claim node for every factual claim it traces, regardless of pass/fail outcome | VERIFIED | Step 8b executes at line 79, before step 9 (audit report write at line 100) and before pass/fail determination — always runs |
| 2 | Each claim node carries edges to source files and canonical figure IDs | VERIFIED | `source_files` field (line 87) and `figure_ids` field (line 88) both defined in step 8b schema |
| 3 | Graph write failures do not block audit promotion | VERIFIED | "do not fail the audit" in step 8b (lines 92, 98); Common Failure Modes table entry at line 236 reinforces this |
| 4 | Re-auditing the same draft overwrites existing claim nodes instead of duplicating them | VERIFIED | Deduplication by `phase` + `section` + `text` equality at line 94 |
| 5 | Running /research:init creates an empty claim-graph.json alongside canonical-figures.json | VERIFIED | init/SKILL.md line 686: write `research/reference/claim-graph.json` with `{"claims": []}` immediately after canonical-figures.json copy |
| 6 | Init verification step confirms claim-graph.json exists | VERIFIED | init/SKILL.md line 732: `reference/claim-graph.json` in Step 6 verification checklist |
| 7 | Research-integrity agent checks claim graph consistency when reviewing phase outputs | VERIFIED | research-integrity.md line 63: `### 9. Claim Graph Consistency` with advisory flag pattern and graceful absence handling |
| 8 | The CLAUDE.md directory tree generated by init includes claim-graph.json | VERIFIED | init/SKILL.md lines 409-410: canonical-figures.json uses `├──`, claim-graph.json uses `└──` as final reference/ entry |

**Score:** 8/8 truths verified

### Roadmap Success Criteria

| # | Success Criterion | Status | Evidence |
|---|-------------------|--------|----------|
| 1 | A claim extracted during audit-claims is recorded as a node with a unique identifier | VERIFIED | Step 8b defines `id` as sequential prefix (c001...) + slug; example `c001-market-size-exceeds-four` at line 82 |
| 2 | Each claim node carries edges pointing to the source note(s) that support it | VERIFIED | `source_files` field = array of note filenames traced in step 5 (line 87) |
| 3 | Each claim node carries an edge to any canonical figure it references (where applicable) | VERIFIED | `figure_ids` field = array of canonical-figures.json IDs, defaults to `[]` if none (line 88) |
| 4 | The claim graph persists in a queryable structure within the project directory | VERIFIED | Persists as `research/reference/claim-graph.json` — valid JSON with `claims` array; verify-after-write pattern at lines 96-98 |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.claude/commands/research/audit-claims/SKILL.md` | Step 8b claim graph write logic | VERIFIED | Step 8b at line 79, all 9 schema fields, deduplication, non-blocking error handling, failure mode table entry at line 236 |
| `.claude/commands/research/init/SKILL.md` | claim-graph.json scaffolding in Step 5, verification in Step 6, directory tree update | VERIFIED | 3 occurrences: line 686 (scaffold), line 410 (directory tree), line 732 (verification checklist) |
| `.claude/agents/research-integrity.md` | Check 9 — Claim Graph Consistency | VERIFIED | Check 9 at line 63; CLAIM GRAPH INCONSISTENCY flag at line 67; advisory non-blocking at line 68; trigger bullet at line 78 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `.claude/commands/research/audit-claims/SKILL.md` | `research/reference/claim-graph.json` | step 8b reads then writes claim-graph.json | VERIFIED | Lines 79-98: read, create-if-absent, deduplicate, write, verify-after-write all present |
| `.claude/commands/research/init/SKILL.md` | `research/reference/claim-graph.json` | Step 5 Other Files scaffolding write | VERIFIED | Line 686: write with `{"claims": []}` initial content |
| `.claude/agents/research-integrity.md` | `research/reference/claim-graph.json` | Check 9 reads claim-graph.json and compares against audit reports | VERIFIED | Line 65: reads file; graceful skip if absent; compares confidence_tier per claim node |

### Data-Flow Trace (Level 4)

These are agent instruction files (SKILL.md, agent markdown), not components rendering dynamic data. Level 4 data-flow trace does not apply — the "data" is the agent's written instructions and there is no runtime rendering path to trace statically.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Step 8b is present and positioned after 8a and before 9 | `grep -n "^8a\.\|^8b\.\|^9\." audit-claims/SKILL.md` | lines 55 (8a), 79 (8b), 100 (9) | PASS |
| All 9 claim node schema fields defined | `grep -c "source_files\|figure_ids\|confidence_tier\|evidence_directness\|source_count" audit-claims/SKILL.md` | 5 distinct fields confirmed in step 8b | PASS |
| init SKILL.md has 3 claim-graph.json insertions | `grep -c "claim-graph.json" init/SKILL.md` | 3 | PASS |
| research-integrity.md has check 9 | `grep -n "### 9. Claim Graph Consistency" research-integrity.md` | line 63 | PASS |
| Commits referenced in summaries exist in git | `git log --oneline \| grep -E "18d7831\|9639ea2\|cd5875d"` | all 3 hashes found | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| TRACE-01 | 11-01-PLAN.md, 11-02-PLAN.md | System represents each claim as a graph node with edges to its source note(s) and any canonical figure it references | SATISFIED | Step 8b in audit-claims SKILL.md writes nodes with source_files and figure_ids fields; init scaffolds the file; integrity agent checks consistency |

No orphaned requirements — REQUIREMENTS.md traceability table assigns only TRACE-01 to Phase 11, and both plans declare only TRACE-01.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.claude/commands/research/init/SKILL.md` | 577 | `placeholder` keyword | Info | Part of Step 4 source-standards template customization (pre-existing, unrelated to Phase 11). Not a stub for claim-graph work. |

No blockers or warnings found in Phase 11 modified sections.

### Human Verification Required

#### 1. Init scaffolds claim-graph.json at runtime

**Test:** Run `/research:init` on a new project directory. After init completes, check `research/reference/claim-graph.json`.
**Expected:** File exists with content `{"claims": []}` — valid JSON, empty claims array.
**Why human:** The write instruction exists in init/SKILL.md but only runtime execution confirms the agent follows it. Static analysis cannot simulate agent tool calls.

#### 2. Audit-claims writes and deduplicates claim nodes

**Test:** Run `/research:audit-claims` on a draft with 3+ distinct factual claims. Inspect `research/reference/claim-graph.json`. Then re-run the same audit. Inspect again.
**Expected:** First run: N claim nodes with all 9 schema fields populated (id with sequential prefix + slug, text, phase, section, confidence_tier, source_files as array, figure_ids as array, evidence_directness, source_count). Second run: same N nodes, no duplicates — nodes overwritten by phase+section+text match.
**Why human:** Step 8b is complete behavioral instruction but only agent execution can confirm all 9 fields are written, the deduplication condition fires correctly, and the non-blocking error path actually continues past write failures.

### Gaps Summary

No gaps found. All 8 must-have truths are verified, all 4 roadmap success criteria are satisfied, all 3 artifact paths exist with substantive content, all 3 key links are wired, and TRACE-01 is fully covered. Human verification is needed only to confirm runtime agent execution of the written instructions — the instruction text itself is complete.

---

_Verified: 2026-04-20_
_Verifier: Claude (gsd-verifier)_
