---
phase: 13-academic-evidence-layer-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/reference/discovery/channel-playbooks/academic.md
  - .claude/commands/research/discover/SKILL.md
autonomous: true
requirements: [DISC-01, DISC-02]
must_haves:
  truths:
    - "Academic channel queries Crossref alongside OpenAlex and returns DOI/author/citation metadata that OpenAlex missed"
    - "Papers with is_oa=false are looked up via Unpaywall inline during discovery and upgraded to ACCESSIBLE when a legal OA copy exists"
    - "All three academic APIs degrade gracefully when unavailable — skip silently, do not block"
    - "Crossref results are deduplicated against OpenAlex results by DOI match"
  artifacts:
    - path: ".claude/reference/discovery/channel-playbooks/academic.md"
      provides: "Crossref query templates, Unpaywall lookup logic, dual-API degradation, deduplication rules"
      contains: "API 2 — Crossref"
    - path: ".claude/commands/research/discover/SKILL.md"
      provides: "Unpaywall status upgrade note in Step 5"
      contains: "Unpaywall"
  key_links:
    - from: ".claude/reference/discovery/channel-playbooks/academic.md"
      to: "https://api.crossref.org/works"
      via: "curl query templates"
      pattern: "api.crossref.org"
    - from: ".claude/reference/discovery/channel-playbooks/academic.md"
      to: "https://api.unpaywall.org/v2/"
      via: "DOI lookup template"
      pattern: "api.unpaywall.org"
    - from: ".claude/commands/research/discover/SKILL.md"
      to: ".claude/reference/discovery/channel-playbooks/academic.md"
      via: "status upgrade reference"
      pattern: "academic.md"
---

<objective>
Extend the academic channel playbook with Crossref and Unpaywall API integrations, and update the discover skill's Step 5 to reflect the Unpaywall status upgrade path.

Purpose: DISC-01 requires Crossref alongside OpenAlex to fill metadata gaps (DOI, author, citation count). DISC-02 requires Unpaywall to find legal OA copies of paywalled papers. Both integrations live in the academic playbook following the dual-API pattern from regulatory.md.

Output: Extended academic.md with three APIs (OpenAlex, Crossref, Unpaywall), query templates, degradation chains, rate limits, example responses, deduplication rules, and a minor note addition to discover/SKILL.md Step 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-academic-evidence-layer-expansion/13-CONTEXT.md
@.planning/phases/13-academic-evidence-layer-expansion/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend academic.md with Crossref and Unpaywall integrations</name>
  <files>.claude/reference/discovery/channel-playbooks/academic.md</files>
  <read_first>
    - .claude/reference/discovery/channel-playbooks/academic.md
    - .claude/reference/discovery/channel-playbooks/regulatory.md
    - .planning/phases/13-academic-evidence-layer-expansion/13-PATTERNS.md
    - .planning/phases/13-academic-evidence-layer-expansion/13-CONTEXT.md
  </read_first>
  <action>
Extend the existing academic.md playbook with Crossref and Unpaywall. Follow the dual-API pattern from regulatory.md (per 13-PATTERNS.md). Do NOT restructure or rename existing OpenAlex content.

**Section 2 (Tool Configuration) — add after existing OpenAlex entry:**

```markdown
**API 2 — Crossref:**
- Base URL: `https://api.crossref.org/works`
- Authentication: None required — polite pool via `mailto` query parameter (same as OpenAlex convention)
- Response format: JSON
- Returns: Paper metadata including DOI, title, authors, citation count (is-referenced-by-count), publication date, and primary URL

**API 3 — Unpaywall:**
- Base URL: `https://api.unpaywall.org/v2/{doi}`
- Authentication: None required — email parameter required for identification: `?email={email}`
- Response format: JSON
- Returns: Open access status, best OA location URL, license, and OA type (gold/green/hybrid/bronze/closed)
```

**Section 3 (Query Templates) — add new heading groups after existing Template C:**

```markdown
### Crossref Query Templates

### Template D — Crossref Topic Search
Find papers on a subject via Crossref, sorted by relevance:

```bash
curl -s "https://api.crossref.org/works?query={topic}&rows=8&sort=relevance&order=desc&mailto={email}"
```

Substitute: `{topic}` = search phrase (URL-encode spaces as `+`), `{email}` = contact email for polite pool.

### Template E — Crossref Author Search
Find papers by a specific author:

```bash
curl -s "https://api.crossref.org/works?query.author={author_name}&rows=8&sort=published&order=desc&mailto={email}"
```

Substitute: `{author_name}` = author name (URL-encode spaces as `+`), `{email}` = contact email.

### Template F — Crossref DOI Lookup
Retrieve full metadata for a known DOI:

```bash
curl -s "https://api.crossref.org/works/{doi}?mailto={email}"
```

Substitute: `{doi}` = full DOI (e.g., `10.1038/s41586-020-2649-2`), `{email}` = contact email.

### Unpaywall Lookup

### Template G — Unpaywall OA Check
Check if a paywalled paper has a legal open-access copy:

```bash
curl -s "https://api.unpaywall.org/v2/{doi}?email={email}"
```

Substitute: `{doi}` = full DOI (e.g., `10.1038/s41586-020-2649-2`), `{email}` = contact email.

**Trigger condition:** Call only when a paper has `is_oa=false` (from OpenAlex) or no OA indicator (from Crossref). Do not call for papers already known to be open access. Bounded by the existing 8-result-per-channel cap — worst case 8 Unpaywall calls per query.
```

**Section 6 (Degradation Behavior) — add after existing Tier 4 / WebSearch fallback block, before "Unavailable criteria":**

```markdown
**Crossref degradation (if api.crossref.org unavailable):**

**Tier 2 [Tavily fallback] for Crossref:**
```bash
tvly search "{topic} site:doi.org OR site:crossref.org" --depth advanced --max-results 8 --json
```
Label results: "via tvly (Crossref fallback)"

**Tier 3 [WebSearch fallback] for Crossref:**
```
query: "{topic} DOI academic paper crossref"
```
Label results: "via WebSearch (Crossref fallback)". Warn user that citation metadata will be unavailable.

**Unpaywall degradation (if api.unpaywall.org unavailable):**

If Unpaywall is unavailable (timeout > 15 seconds, HTTP 5xx, HTTP 429), the paper remains DISCOVERED. Skip silently. Log a one-line channel status note: `Unpaywall: unavailable — N papers remain DISCOVERED`. Do not retry. Do not block. Do not fall back to another service — there is no equivalent free OA lookup service.
```

Update the "Unavailable criteria" block to add Crossref mention:
```markdown
**Unavailable criteria:** Trigger next tier when:
- HTTP 5xx response from api.openalex.org or api.crossref.org (or non-zero exit from CLI)
- Request timeout > 15 seconds
- HTTP 429 rate limit (without mailto parameter — add mailto and retry once before falling back)
```

**Section 7 (Rate Limits) — add after existing OpenAlex entries:**

```markdown
**Crossref polite pool (with mailto parameter):** 50 requests/second
**Crossref without mailto:** Rate limited to ~1 request/second with aggressive throttling.
**Unpaywall (with email parameter):** 100,000 requests/day (practically unlimited for research use). Required: `email` parameter in every request.
```

**New Section — "Example Response" additions — add immediately after existing "Example Response" section (after "Fields to extract per result" for OpenAlex):**

```markdown
### Crossref Example Response

Trimmed Crossref JSON response for a works query. The discover skill should extract these fields:

```json
{
  "message": {
    "items": [
      {
        "DOI": "10.1038/s41586-020-2649-2",
        "title": ["Language models show human-like content effects on reasoning"],
        "author": [
          {"given": "Ishita", "family": "Dasgupta"}
        ],
        "is-referenced-by-count": 245,
        "published": {"date-parts": [[2023, 1, 15]]},
        "resource": {"primary": {"URL": "https://doi.org/10.1038/s41586-020-2649-2"}}
      }
    ]
  }
}
```

**Fields to extract per result:**
- `DOI` — canonical identifier (use for deduplication against OpenAlex)
- `title[0]` — paper title (Crossref wraps in array)
- `author[].given` + `author[].family` — author names
- `is-referenced-by-count` — citation count
- `published.date-parts[0]` — publication date as [year, month, day] array
- `resource.primary.URL` — landing page URL

### Unpaywall Example Response

Trimmed Unpaywall JSON response for a DOI lookup:

```json
{
  "doi": "10.1038/s41586-020-2649-2",
  "is_oa": true,
  "best_oa_location": {
    "url": "https://europepmc.org/articles/pmc7888567",
    "license": "cc-by",
    "version": "publishedVersion"
  },
  "oa_status": "green"
}
```

**Fields to extract:**
- `doi` — confirms lookup target
- `is_oa` — true means legal OA copy found; upgrade source status to ACCESSIBLE
- `best_oa_location.url` — direct OA URL (use as the accessible link in candidates)
- `best_oa_location.license` — license type (cc-by, cc-by-nc, etc.)
- `oa_status` — gold/green/hybrid/bronze/closed (informational for credibility notes)
```

**New Section 8 — add after Section 7 (Rate Limits) as a new numbered section. Rename existing "Example Response" to Section 9 if numbering conflicts, OR insert Section 8 between Rate Limits and Example Response:**

```markdown
## 8. Deduplication and Priority Rules

**Query order:** OpenAlex first, Crossref second, Unpaywall third (inline during same pass).

**Deduplication by DOI:** If a paper appears in both OpenAlex and Crossref results (matched by DOI string equality after normalization to lowercase), the OpenAlex entry is kept. The Crossref entry is silently dropped. This mirrors the URL deduplication in the discover skill's Step 4.

**Data merge:** After deduplication, Crossref enriches missing fields on the surviving OpenAlex entries:
- If OpenAlex entry has no DOI but Crossref matched by title → add DOI from Crossref
- If OpenAlex entry has no citation count → use Crossref `is-referenced-by-count`
- If OpenAlex author list is incomplete → supplement from Crossref `author[]`
- OpenAlex data has priority in all cases — Crossref fills gaps only, never overwrites

**Unpaywall trigger:** Called only when a paper has `is_oa=false` (from OpenAlex) or has no OA indicator (from Crossref-only results). Result: if `best_oa_location.url` is present and non-null, upgrade source status from DISCOVERED to ACCESSIBLE. Capped by the existing 8-result-per-channel limit.

**Channel status line:** After all three APIs complete, report a single academic channel status line:
```
Academic: {N} results (OpenAlex: {n1}, Crossref-only: {n2}, Unpaywall upgrades: {n3}) [degraded: {list}]
```
Where `{list}` names any API that was unavailable during this run (empty if all succeeded).
```
  </action>
  <verify>
    <automated>grep -c "api.crossref.org" .claude/reference/discovery/channel-playbooks/academic.md && grep -c "api.unpaywall.org" .claude/reference/discovery/channel-playbooks/academic.md && grep -c "Deduplication and Priority" .claude/reference/discovery/channel-playbooks/academic.md</automated>
  </verify>
  <acceptance_criteria>
    - academic.md contains "API 2 — Crossref" with base URL `https://api.crossref.org/works`
    - academic.md contains "API 3 — Unpaywall" with base URL `https://api.unpaywall.org/v2/{doi}`
    - academic.md contains "Template D — Crossref Topic Search" with curl command to api.crossref.org
    - academic.md contains "Template E — Crossref Author Search"
    - academic.md contains "Template F — Crossref DOI Lookup"
    - academic.md contains "Template G — Unpaywall OA Check" with trigger condition paragraph
    - academic.md contains "Crossref degradation" block with Tavily and WebSearch tiers
    - academic.md contains "Unpaywall degradation" block with "Skip silently" and "Do not retry"
    - academic.md contains "Crossref polite pool (with mailto parameter):" rate limit entry
    - academic.md contains "Unpaywall (with email parameter):" rate limit entry
    - academic.md contains Crossref example JSON with `is-referenced-by-count` field
    - academic.md contains Unpaywall example JSON with `best_oa_location` object
    - academic.md contains "## 8. Deduplication and Priority Rules" section
    - academic.md contains "OpenAlex first, Crossref second, Unpaywall third"
    - academic.md contains "OpenAlex data has priority in all cases"
    - academic.md contains channel status line format with "Unpaywall upgrades" counter
    - Existing OpenAlex content (Templates A, B, C; Section 2 base URL; Section 6 tier chain) remains unchanged
  </acceptance_criteria>
  <done>academic.md has three API integrations (OpenAlex, Crossref, Unpaywall) with query templates, degradation chains, rate limits, example responses, and deduplication rules following the dual-API pattern from regulatory.md</done>
</task>

<task type="auto">
  <name>Task 2: Add Unpaywall status upgrade note to discover/SKILL.md Step 5</name>
  <files>.claude/commands/research/discover/SKILL.md</files>
  <read_first>
    - .claude/commands/research/discover/SKILL.md
    - .planning/phases/13-academic-evidence-layer-expansion/13-PATTERNS.md
  </read_first>
  <action>
In `.claude/commands/research/discover/SKILL.md`, make two targeted edits to Step 5 (the source status determination section, around lines 128-136).

**Edit 1:** In the ACCESSIBLE bullet list, add Unpaywall after the OpenAlex example. Change:

```
(e.g., OpenAlex `is_oa=true`, Wikipedia, government data portals, arxiv.org, SEC EDGAR filings)
```

To:

```
(e.g., OpenAlex `is_oa=true`, Unpaywall `best_oa_location.url` present and non-null, Wikipedia, government data portals, arxiv.org, SEC EDGAR filings)
```

**Edit 2:** At the end of the DISCOVERED bullet (after the existing text about defaulting to DISCOVERED when uncertain), add this sentence:

```
Academic papers with `is_oa=false` from OpenAlex or Crossref begin as DISCOVERED; Unpaywall lookup (per academic.md Section 8) may upgrade them to ACCESSIBLE inline during the same discovery pass if a legal OA copy is found.
```

No other changes to discover/SKILL.md.
  </action>
  <verify>
    <automated>grep "Unpaywall" .claude/commands/research/discover/SKILL.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - discover/SKILL.md Step 5 ACCESSIBLE bullet contains "Unpaywall `best_oa_location.url` present and non-null"
    - discover/SKILL.md Step 5 DISCOVERED section contains "Academic papers with `is_oa=false` from OpenAlex or Crossref begin as DISCOVERED"
    - discover/SKILL.md contains "Unpaywall lookup (per academic.md Section 8)"
    - discover/SKILL.md contains "may upgrade them to ACCESSIBLE inline during the same discovery pass"
    - No other sections of discover/SKILL.md are modified (Step 1-4 and Step 6+ remain unchanged)
  </acceptance_criteria>
  <done>discover/SKILL.md Step 5 references Unpaywall as a source of ACCESSIBLE status and explains the inline upgrade path from DISCOVERED to ACCESSIBLE for paywalled academic papers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Crossref API | Untrusted HTTP responses parsed for metadata |
| Agent -> Unpaywall API | Untrusted HTTP responses parsed for OA URLs |
| Unpaywall URL -> User browser | OA URLs could be malicious if Unpaywall data compromised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Spoofing | Crossref API response | accept | Crossref is a well-established DOI registry; responses are metadata-only (no executable content). Low risk. |
| T-13-02 | Tampering | Unpaywall best_oa_location.url | accept | URLs are presented to user in candidates file for manual review before processing. No auto-fetch of OA content. |
| T-13-03 | Denial of Service | Rate limiting by Crossref/Unpaywall | mitigate | Playbook specifies mailto/email parameters for polite pool access; degradation chain skips silently on 429/timeout |
| T-13-04 | Information Disclosure | Email in API requests | accept | Contact email is required by both APIs for polite access; this is standard academic API convention, not a secret |
</threat_model>

<verification>
1. `grep -c "api.crossref.org" .claude/reference/discovery/channel-playbooks/academic.md` returns >= 3
2. `grep -c "api.unpaywall.org" .claude/reference/discovery/channel-playbooks/academic.md` returns >= 2
3. `grep "Unpaywall" .claude/commands/research/discover/SKILL.md` returns at least 2 matches
4. `grep "Deduplication and Priority" .claude/reference/discovery/channel-playbooks/academic.md` returns 1 match
5. Existing OpenAlex templates (A, B, C) still present in academic.md
</verification>

<success_criteria>
- Academic channel playbook contains three API integrations with complete query templates, degradation chains, rate limits, and example responses
- Crossref results are deduplicated against OpenAlex by DOI with OpenAlex having priority
- Unpaywall is triggered only for is_oa=false papers, upgrades status inline during discovery
- Graceful degradation documented for all three APIs (Crossref cascades; Unpaywall skips silently)
- discover/SKILL.md Step 5 references Unpaywall as a named ACCESSIBLE source
</success_criteria>

<output>
After completion, create `.planning/phases/13-academic-evidence-layer-expansion/13-01-SUMMARY.md`
</output>
