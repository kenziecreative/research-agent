---
phase: 13-academic-evidence-layer-expansion
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/reference/coverage-assessment-guide.md
  - .claude/commands/research/check-gaps/SKILL.md
autonomous: true
requirements: [TRACE-05]
must_haves:
  truths:
    - "Gap analysis classifies sources as Direct, Adjacent, Contradicts, or None — four tiers, not three"
    - "A question with 0 Direct sources and at least 1 Contradicts source gets coverage status 'Evidence Against' — not 'Not Started'"
    - "Highest-priority gaps list distinguishes 'Not Started' (discovery targets) from 'Evidence Against' (synthesis challenges)"
    - "Contradicts sources appear in a separate per-question section in gaps.md output"
  artifacts:
    - path: ".claude/reference/coverage-assessment-guide.md"
      provides: "Contradicts classification definition and Evidence Against status definition"
      contains: "Contradicts"
    - path: ".claude/commands/research/check-gaps/SKILL.md"
      provides: "Four-tier classification in step 6, Evidence Against status, dashboard counters, priority list extension"
      contains: "Evidence Against"
  key_links:
    - from: ".claude/commands/research/check-gaps/SKILL.md"
      to: ".claude/reference/coverage-assessment-guide.md"
      via: "step 4 reads guide for classification criteria"
      pattern: "coverage-assessment-guide.md"
    - from: ".claude/commands/research/check-gaps/SKILL.md"
      to: "research/gaps.md"
      via: "step 7 writes gaps file with Evidence Against status"
      pattern: "Evidence Against"
---

<objective>
Extend gap analysis with the Contradicts classification and Evidence Against coverage status, enabling the system to distinguish between "no sources found" and "sources actively contradict the hypothesis."

Purpose: TRACE-05 requires gap analysis to distinguish absence of evidence from evidence against. Currently all uncovered questions get "Not Started" — this loses the critical signal that counter-evidence exists and the user faces a synthesis challenge rather than a discovery gap.

Output: Updated coverage-assessment-guide.md with four-tier classification and Evidence Against status; updated check-gaps/SKILL.md with classification logic, output format, dashboard counters, priority ordering, guardrail, and failure mode entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-academic-evidence-layer-expansion/13-CONTEXT.md
@.planning/phases/13-academic-evidence-layer-expansion/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend coverage-assessment-guide.md with Contradicts classification and Evidence Against status</name>
  <files>.claude/reference/coverage-assessment-guide.md</files>
  <read_first>
    - .claude/reference/coverage-assessment-guide.md
    - .planning/phases/13-academic-evidence-layer-expansion/13-PATTERNS.md
    - .planning/phases/13-academic-evidence-layer-expansion/13-CONTEXT.md
  </read_first>
  <action>
Make four targeted edits to coverage-assessment-guide.md. Follow the exact copy instructions from 13-PATTERNS.md.

**Edit 1 — Match Classification section (around line 27):**

Change "one of three tiers" to "one of four tiers" in the intro sentence.

Insert the Contradicts entry between the Adjacent bullet and the None bullet:

```markdown
- **Contradicts:** Source actively opposes or contradicts the research question's hypothesis — same topic as the question but arguing the opposite conclusion. Distinguished from Adjacent (which is a different topic) and from Direct (which supports the question). Display with explanation: "Contradicts [phase question] by [brief description]". Contradicts matches do **not** count toward Direct coverage status, but do trigger "Evidence Against" status when no Direct sources exist.
```

Add a new critical rule paragraph immediately after the existing "Critical rule: Adjacent matches do not contribute..." paragraph:

```markdown
**Critical rule:** Contradicts is not Adjacent. A source arguing against the hypothesis is Contradicts. A source about a related-but-different topic is Adjacent. Never use Adjacent as a catch-all for inconvenient sources — the distinction is load-bearing for Evidence Against detection.
```

**Edit 2 — Coverage Status Definitions section (around line 48-55):**

Insert a new status bullet after "Not started" and before "Addressed but unbalanced":

```markdown
- **Evidence Against:** No source notes have a **Direct** match for this question, but at least one source note is classified **Contradicts** — meaning active counter-evidence exists. Distinguished from "Not started" (which means no sources at all, for or against). An Evidence Against question is a synthesis challenge: the user must address the contradiction, not find more sources. Discovery will not resolve it.
```

**Edit 3 — Coverage Assessment Workflow section (around line 82-90):**

In step 1, change "classify each as Direct, Adjacent, or None" to "classify each as Direct, Adjacent, Contradicts, or None".

Insert a new step 2 after existing step 1, and shift existing steps 2-6 to become steps 3-7:

```markdown
2. For Contradicts classifications: verify the source actively argues against the hypothesis (not merely presenting a different facet). If in doubt, use Adjacent — only use Contradicts when the source's conclusion directly opposes the research question.
```

**Edit 4 — Source Diversity Requirements table (no change needed — Contradicts is orthogonal to diversity). Skip this section.**

The final file should have:
- Four tiers in Match Classification: Direct, Adjacent, Contradicts, None
- Two critical rules (one for Adjacent, one for Contradicts)
- Five coverage statuses: Complete, Partial, Not started, Evidence Against, Addressed but unbalanced
- Seven steps in the workflow (was 6)
  </action>
  <verify>
    <automated>grep -c "Contradicts" .claude/reference/coverage-assessment-guide.md && grep "one of four tiers" .claude/reference/coverage-assessment-guide.md && grep "Evidence Against" .claude/reference/coverage-assessment-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - coverage-assessment-guide.md Match Classification says "one of four tiers" (not "three")
    - coverage-assessment-guide.md contains "**Contradicts:** Source actively opposes or contradicts the research question's hypothesis"
    - coverage-assessment-guide.md contains "Contradicts matches do **not** count toward Direct coverage status"
    - coverage-assessment-guide.md contains critical rule "Contradicts is not Adjacent"
    - coverage-assessment-guide.md Coverage Status Definitions contains "**Evidence Against:**" bullet
    - coverage-assessment-guide.md Evidence Against definition contains "synthesis challenge"
    - coverage-assessment-guide.md Evidence Against definition contains "Distinguished from \"Not started\""
    - coverage-assessment-guide.md workflow step 1 contains "Direct, Adjacent, Contradicts, or None"
    - coverage-assessment-guide.md contains new step 2 with "verify the source actively argues against the hypothesis"
    - Existing Direct, Adjacent, None definitions remain unchanged
    - Existing Complete, Partial, Not started, Addressed but unbalanced definitions remain unchanged
  </acceptance_criteria>
  <done>coverage-assessment-guide.md defines four-tier classification (Direct/Adjacent/Contradicts/None) and five coverage statuses (Complete/Partial/Not started/Evidence Against/Addressed but unbalanced) with clear distinction rules</done>
</task>

<task type="auto">
  <name>Task 2: Extend check-gaps/SKILL.md with Contradicts classification, Evidence Against status, and output format</name>
  <files>.claude/commands/research/check-gaps/SKILL.md</files>
  <read_first>
    - .claude/commands/research/check-gaps/SKILL.md
    - .claude/reference/coverage-assessment-guide.md
    - .planning/phases/13-academic-evidence-layer-expansion/13-PATTERNS.md
    - .planning/phases/13-academic-evidence-layer-expansion/13-CONTEXT.md
  </read_first>
  <action>
Make six targeted edits to check-gaps/SKILL.md. Follow the exact copy instructions from 13-PATTERNS.md.

**Edit 1 — Process step 4 (line ~17):**

Change the coverage-assessment-guide.md reference from "(Direct/Adjacent/None)" to "(Direct/Adjacent/Contradicts/None)" so the step 4 read instruction reflects the updated guide.

**Edit 2 — Process step 6 (lines ~19-24):**

In step 6a, change "Direct, Adjacent, or None" to "Direct, Adjacent, Contradicts, or None".

Add a new sub-item after 6b (insert as 6c, shift existing 6c-6e to 6d-6f):

```
   c. For Contradicts matches: note with a one-line explanation of what the source opposes. Do not count Contradicts sources toward Direct coverage. Do not collapse with Adjacent.
```

Add a new sub-item at the end (as 6g, after the shifted 6f which was the lopsided flag):

```
   g. If a question has 0 Direct sources and at least 1 Contradicts source, assign coverage status "Evidence Against" (not "Not Started"). Evidence Against means the question has active counter-evidence, not an absence of evidence.
```

**Edit 3 — Step 7 output format, Dashboard section (around line 28-34):**

Add two new dashboard counters after the existing "Adjacent-only matches" line:

```
   - **Evidence Against:** N questions (active counter-evidence, no Direct sources)
   - **Contradicts matches:** N total (across all questions)
```

**Edit 4 — Step 7 output format, Per-question detail (around lines 38-50):**

In the Coverage status bracket, change `[Complete/Partial/Not Started/Addressed but unbalanced]` to `[Complete/Partial/Not Started/Evidence Against/Addressed but unbalanced]`.

Add a new section block after the "Adjacent sources" section:

```
   **Contradicts sources:** <- section only if Contradicts matches exist
   - [source-note-filename]: Contradicts [phase question] by [brief description of what it opposes]
```

**Edit 5 — Highest-priority gaps list (around lines 87-99):**

Extend the status bracket in the list format line from `[Not Started | Lopsided | Adjacent-only]` to `[Not Started | Lopsided | Adjacent-only | Evidence Against]`.

Insert a new criticality level 2 (shift existing items 2-4 to become 3-5):

```
2. Evidence Against questions on phases whose Verify step is the next cycle step (synthesis is imminent; the user must address the contradiction before drafting).
```

After the criticality list (before the "If more than 10 gaps..." line), add:

```
Note: "Not Started" questions are discovery targets — run /research:discover to fill them. "Evidence Against" questions are synthesis challenges — the user must address the contradiction in the draft, not find more sources.
```

**Edit 6 — Guardrails and Common Failure Modes:**

Add Guardrail 10 (after existing Guardrail 9):

```
10. Contradicts classification is not a subcategory of Adjacent. A source that actively opposes the research question's hypothesis is Contradicts — not Adjacent. Adjacent means "related but different topic." Contradicts means "same topic, opposing conclusion." Do not collapse the two. A question with 1 Direct and 1 Contradicts source is "Addressed but unbalanced" (the contradiction is surfaced, not hidden in Adjacent).
```

Add a new row to the Common Failure Modes table:

```
| Collapsing Contradicts into Adjacent or None | Contradicts is a distinct fourth classification. A source that actively argues against the research question's hypothesis is Contradicts. It must appear in the "Contradicts sources" section of the per-question detail and trigger "Evidence Against" status when no Direct sources exist. Dropping it to Adjacent or None hides active counter-evidence from the user. |
```
  </action>
  <verify>
    <automated>grep -c "Contradicts" .claude/commands/research/check-gaps/SKILL.md && grep -c "Evidence Against" .claude/commands/research/check-gaps/SKILL.md && grep "Guardrail" .claude/commands/research/check-gaps/SKILL.md | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - check-gaps/SKILL.md step 4 reference contains "(Direct/Adjacent/Contradicts/None)"
    - check-gaps/SKILL.md step 6a contains "Direct, Adjacent, Contradicts, or None"
    - check-gaps/SKILL.md contains sub-item "For Contradicts matches: note with a one-line explanation"
    - check-gaps/SKILL.md contains sub-item about assigning "Evidence Against" when "0 Direct sources and at least 1 Contradicts"
    - check-gaps/SKILL.md Dashboard contains "**Evidence Against:** N questions"
    - check-gaps/SKILL.md Dashboard contains "**Contradicts matches:** N total"
    - check-gaps/SKILL.md per-question detail contains "**Contradicts sources:**"
    - check-gaps/SKILL.md Coverage bracket contains "Evidence Against"
    - check-gaps/SKILL.md highest-priority gaps status bracket contains "Evidence Against"
    - check-gaps/SKILL.md criticality list has "Evidence Against questions on phases whose Verify step is the next cycle step" as item 2
    - check-gaps/SKILL.md contains Note distinguishing "Not Started" (discovery targets) from "Evidence Against" (synthesis challenges)
    - check-gaps/SKILL.md contains Guardrail 10 about Contradicts not being a subcategory of Adjacent
    - check-gaps/SKILL.md Common Failure Modes table contains "Collapsing Contradicts into Adjacent or None" row
    - Existing guardrails 1-9 remain unchanged
    - Existing failure mode rows remain unchanged
  </acceptance_criteria>
  <done>check-gaps/SKILL.md implements four-tier classification in its process loop, outputs Contradicts sources in per-question detail, reports Evidence Against in dashboard and priority list, and has guardrails preventing Contradicts/Adjacent conflation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source notes -> Classification | Agent classifies user-provided source notes; misclassification is a data integrity risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | Contradicts classification accuracy | mitigate | Guardrail 10 + critical rule in coverage-assessment-guide.md define precise boundary between Contradicts and Adjacent; Common Failure Mode entry adds redundant guidance |
| T-13-06 | Repudiation | Evidence Against hiding gaps | mitigate | Evidence Against surfaces counter-evidence visibly in dashboard + priority list; cannot be silently dropped to Not Started |
| T-13-07 | Information Disclosure | N/A | accept | No new sensitive data flows; classification operates on existing source notes |
</threat_model>

<verification>
1. `grep -c "Contradicts" .claude/reference/coverage-assessment-guide.md` returns >= 5
2. `grep -c "Contradicts" .claude/commands/research/check-gaps/SKILL.md` returns >= 5
3. `grep -c "Evidence Against" .claude/commands/research/check-gaps/SKILL.md` returns >= 4
4. `grep "one of four tiers" .claude/reference/coverage-assessment-guide.md` returns 1 match
5. `grep "Evidence Against" .claude/reference/coverage-assessment-guide.md` returns at least 1 match
6. Existing three classification tiers (Direct, Adjacent, None) still present in both files
</verification>

<success_criteria>
- Gap analysis uses four-tier classification: Direct, Adjacent, Contradicts, None
- Evidence Against status is distinct from Not Started — requires 0 Direct + 1+ Contradicts
- Dashboard shows Evidence Against count and total Contradicts matches
- Per-question output has a Contradicts sources section (conditional, like Adjacent)
- Priority list ranks Evidence Against between Not Started and Lopsided
- Guardrails and failure modes prevent Contradicts/Adjacent conflation
- Coverage-assessment-guide.md and check-gaps/SKILL.md are kept in sync on the four-tier taxonomy
</success_criteria>

<output>
After completion, create `.planning/phases/13-academic-evidence-layer-expansion/13-02-SUMMARY.md`
</output>
