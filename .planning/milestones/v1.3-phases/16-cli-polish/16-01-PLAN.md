---
phase: 16-cli-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/reference/prompt-templates.md
  - .claude/commands/research/phase-insight/SKILL.md
  - .claude/commands/research/summarize-section/SKILL.md
  - .claude/commands/research/audit-claims/SKILL.md
autonomous: true
requirements: [UX-01, UX-03]

must_haves:
  truths:
    - "prompt-templates.md contains a CLI Tone Rules section with banned phrases and replacement examples"
    - "The Skills That Use / Do NOT Use lists reflect the new reality where init, discover, process-source, cross-ref, and check-gaps now use the template"
    - "phase-insight, summarize-section, and audit-claims existing transition blocks use unicode box-drawing delimiters, not ASCII hyphens"
  artifacts:
    - path: ".claude/reference/prompt-templates.md"
      provides: "CLI Tone Rules section and updated skills lists"
      contains: "## CLI Tone Rules"
    - path: ".claude/commands/research/phase-insight/SKILL.md"
      provides: "Verified unicode delimiters on existing transition block"
      contains: "───────────────────────────────────────────────────────────"
    - path: ".claude/commands/research/summarize-section/SKILL.md"
      provides: "Verified unicode delimiters on existing transition block"
      contains: "───────────────────────────────────────────────────────────"
    - path: ".claude/commands/research/audit-claims/SKILL.md"
      provides: "Verified unicode delimiters on existing transition block"
      contains: "───────────────────────────────────────────────────────────"
  key_links:
    - from: ".claude/reference/prompt-templates.md"
      to: "all 10 SKILL.md files"
      via: "CLI Tone Rules referenced by every skill's Output section"
      pattern: "CLI Tone Rules"
---

<objective>
Add CLI tone rules to prompt-templates.md (the single reference file all skills read for output formatting) and update its skills lists to reflect the expanded set of skills that now use the transition prompt template. Audit the three skills that already have transition blocks (phase-insight, summarize-section, audit-claims) to verify unicode delimiters and absence of banned phrases.

Purpose: Establishes the shared infrastructure (tone rules, corrected skills lists) that Plan 02's skill-level edits depend on. All skills read prompt-templates.md — getting the rules and lists right here means Plan 02 can reference them directly.

Output: Updated prompt-templates.md with CLI Tone Rules section and corrected skills lists. Three audit-verified skill files with confirmed unicode delimiters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cli-polish/16-CONTEXT.md
@.planning/phases/16-cli-polish/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append CLI Tone Rules section and update skills lists in prompt-templates.md</name>
  <files>.claude/reference/prompt-templates.md</files>
  <read_first>
    - .claude/reference/prompt-templates.md
    - .planning/phases/16-cli-polish/16-CONTEXT.md (decisions D-09 through D-12 define tone rules)
    - .planning/phases/16-cli-polish/16-PATTERNS.md (exact new section content specified)
  </read_first>
  <action>
Two changes to this file:

**Change 1: Update the skills lists (lines 134-149)**

Replace the `## Skills That Use This Template` section (lines 134-140) with:

```markdown
## Skills That Use This Template

- `/research:init` — after Step 7 report (Phase 1 discovery handoff)
- `/research:start-phase` — at end of phase briefing
- `/research:discover` — after discovery completion summary
- `/research:process-source` — after source processing summary
- `/research:cross-ref` — after cross-reference report (context-sensitive)
- `/research:check-gaps` — after gap analysis (context-sensitive)
- `/research:summarize-section` — after draft is written
- `/research:audit-claims` — after phase debrief, on pass only
- `/research:phase-insight` — conditionally, only when the recommendation is unambiguous
- End-of-phase reminder (defined in the assembled CLAUDE.md Section 8)
```

Replace the `## Skills That Do NOT Use This Template` section (lines 142-149) with:

```markdown
## Skills That Do NOT Use This Template

- `/research:progress` — read-only dashboard, no handoff
```

Per D-05: all 10 skills end with a next-action block. Only `progress` remains excluded (it is read-only per the PATTERNS.md analysis and the existing prompt-templates.md line 144-145 rationale).

**Change 2: Append the CLI Tone Rules section after the updated skills lists**

After the `## Skills That Do NOT Use This Template` section, append a `---` divider followed by the new section. The exact content to append (per D-09, D-10, D-11, D-12 and the PATTERNS.md specification):

```markdown
---

## CLI Tone Rules

These rules apply to all CLI skill output: transition text, status messages, next-action context, and `## Output` section content. They do NOT apply to research output written to `research/outputs/`, which follows `writing-standards.md`.

### Banned phrases

Never use these phrases in CLI skill output:

- "it should be noted that"
- "it is worth noting"
- "the evidence suggests" (in CLI status/transition text — permitted in research outputs)
- "it may be the case that"
- "one might consider"
- "this would allow for"
- "in order to facilitate"
- "to that end"

### Replacement style

Write direct present-tense statements of what the command does and why it applies now.

**Banned:** "It is worth noting that additional sources may facilitate better coverage."
**Correct:** "Coverage is thin on 2 questions — run discover to find more sources."

**Banned:** "One might consider running cross-ref at this point in order to facilitate pattern detection."
**Correct:** "Cross-reference is overdue — 6 sources processed since the last run."

The test: could a developer with no context understand the sentence in under two seconds? If not, rewrite it.
```
  </action>
  <verify>
    <automated>grep -c "## CLI Tone Rules" .claude/reference/prompt-templates.md && grep -c "it should be noted that" .claude/reference/prompt-templates.md && grep -c "/research:progress" .claude/reference/prompt-templates.md && grep -c "/research:init" .claude/reference/prompt-templates.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/reference/prompt-templates.md contains the heading `## CLI Tone Rules`
    - .claude/reference/prompt-templates.md contains all 8 banned phrases listed under `### Banned phrases`
    - .claude/reference/prompt-templates.md contains `### Replacement style` with both Banned/Correct example pairs
    - The `## Skills That Use This Template` section lists 10 entries: init, start-phase, discover, process-source, cross-ref, check-gaps, summarize-section, audit-claims, phase-insight, End-of-phase reminder
    - The `## Skills That Do NOT Use This Template` section lists only `/research:progress`
    - Lines 1-131 of the file (the original Transition Prompt Template content and examples) are unchanged
    - The `---` divider separates the new CLI Tone Rules section from the skills lists above it
  </acceptance_criteria>
  <done>prompt-templates.md has CLI Tone Rules section with all banned phrases and replacement examples, and updated skills lists reflecting the 9 skills that now use the template (plus end-of-phase reminder) and only progress excluded</done>
</task>

<task type="auto">
  <name>Task 2: Audit existing transition blocks in phase-insight, summarize-section, and audit-claims for unicode delimiters and banned phrases</name>
  <files>.claude/commands/research/phase-insight/SKILL.md, .claude/commands/research/summarize-section/SKILL.md, .claude/commands/research/audit-claims/SKILL.md</files>
  <read_first>
    - .claude/commands/research/phase-insight/SKILL.md
    - .claude/commands/research/summarize-section/SKILL.md
    - .claude/commands/research/audit-claims/SKILL.md
    - .planning/phases/16-cli-polish/16-PATTERNS.md (audit requirements for these files)
  </read_first>
  <action>
For each of the three files, verify two things:

1. **Unicode delimiter check:** Every `▶ NEXT:` block delimiter must be the unicode box-drawing character `─` (U+2500), NOT ASCII hyphen `-`. The delimiter line is: `───────────────────────────────────────────────────────────` (59 unicode box-drawing characters). If any delimiter uses ASCII hyphens or a different character, replace it with the correct unicode line.

2. **Banned phrase scan:** Search each file's `## Output` section, `## Common Failure Modes` table, and any inline `▶ NEXT:` block text for the 8 banned phrases from D-10:
   - "it should be noted that"
   - "it is worth noting"  
   - "the evidence suggests" (in CLI status text only — this phrase is permitted in research output context)
   - "it may be the case that"
   - "one might consider"
   - "this would allow for"
   - "in order to facilitate"
   - "to that end"

   If any banned phrase is found in CLI-facing text (Output section, Common Failure Modes, transition blocks), rewrite it using direct present-tense language per D-11. Do NOT change phrases that appear in research output context (e.g., writing-standards references).

Per the PATTERNS.md analysis:
- **phase-insight/SKILL.md:** The existing transition block (lines 67-77) is already correct format. Verify delimiter characters only. The conditional rendering rule at lines 65-80 should be preserved as-is.
- **summarize-section/SKILL.md:** The existing `▶ NEXT:` block (lines 169-179) is already correct. Verify delimiters and scan for banned phrases.
- **audit-claims/SKILL.md:** The existing pass-case block (lines 224-235) and fail-case behavior (lines 262-266) are already correct. Verify delimiters and scan debrief prose (lines 204-217) for banned phrases.

If all checks pass with no changes needed, note that in the summary. Do not make unnecessary edits.
  </action>
  <verify>
    <automated>python3 -c "
import re
files = [
    '.claude/commands/research/phase-insight/SKILL.md',
    '.claude/commands/research/summarize-section/SKILL.md',
    '.claude/commands/research/audit-claims/SKILL.md'
]
banned = ['it should be noted that', 'it is worth noting', 'it may be the case that', 'one might consider', 'this would allow for', 'in order to facilitate', 'to that end']
ok = True
for f in files:
    content = open(f).read()
    # Check unicode delimiters exist
    if '───────────────────────────────────────────────────────────' not in content:
        print(f'FAIL: {f} missing unicode delimiter')
        ok = False
    # Check no ASCII hyphen delimiter lines (59+ hyphens)
    if re.search(r'-{59,}', content):
        print(f'FAIL: {f} has ASCII hyphen delimiter')
        ok = False
    for phrase in banned:
        if phrase.lower() in content.lower():
            print(f'FAIL: {f} contains banned phrase: {phrase}')
            ok = False
if ok:
    print('PASS: All 3 files have unicode delimiters and no banned phrases')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - phase-insight/SKILL.md contains `───────────────────────────────────────────────────────────` (unicode U+2500) as delimiter lines around the transition block
    - summarize-section/SKILL.md contains `───────────────────────────────────────────────────────────` (unicode U+2500) as delimiter lines around the transition block
    - audit-claims/SKILL.md contains `───────────────────────────────────────────────────────────` (unicode U+2500) as delimiter lines around both the pass-case and fail-case blocks
    - None of the 3 files contain any of the 8 banned phrases in their Output, Common Failure Modes, or transition block sections
    - No ASCII hyphen delimiter lines (59+ consecutive `-` characters) exist in any of the 3 files
  </acceptance_criteria>
  <done>All three skills with existing transition blocks verified: unicode delimiters confirmed correct, no banned phrases found in CLI-facing output text</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply. This phase edits documentation/instruction files (SKILL.md and prompt-templates.md) that guide Claude's behavior. No user data, no API calls, no authentication, no runtime code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | prompt-templates.md tone rules | accept | File is read-only reference for Claude; no user-facing runtime execution. Version controlled in git. |
</threat_model>

<verification>
1. `grep "## CLI Tone Rules" .claude/reference/prompt-templates.md` returns a match
2. `grep -c "it should be noted" .claude/reference/prompt-templates.md` returns 1 (the banned phrase is listed in the rules, not used in prose)
3. All 3 audited files pass the unicode delimiter and banned phrase verification script
4. The skills lists in prompt-templates.md correctly reflect the D-05 decision (all skills except progress use the template)
</verification>

<success_criteria>
- prompt-templates.md has the CLI Tone Rules section appended with all 8 banned phrases and 2 replacement examples
- prompt-templates.md skills lists updated: 10 entries in "Use", 1 entry in "Do NOT Use"
- phase-insight, summarize-section, and audit-claims pass delimiter and banned-phrase audits
- No existing content in prompt-templates.md lines 1-131 was altered
</success_criteria>

<output>
After completion, create `.planning/phases/16-cli-polish/16-01-SUMMARY.md`
</output>
