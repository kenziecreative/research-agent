---
phase: 06-discover-skill-consistency-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/research/discover/SKILL.md
  - .claude/commands/research/init/SKILL.md
autonomous: true
requirements: [CHAN-02, CHAN-03, CHAN-04, DSKL-02, INIT-02]
gap_closure: true

must_haves:
  truths:
    - "ProPublica URL in discover SKILL.md uses {ein_no_dashes} matching regulatory.md"
    - "Init CLAUDE.md template includes a machine-readable research-type: field"
    - "Academic channel fallback chain in discover SKILL.md matches academic.md section 6 (no Semantic Scholar)"
    - "EDGAR User-Agent in discover SKILL.md matches the value in regulatory.md"
  artifacts:
    - path: ".claude/commands/research/discover/SKILL.md"
      provides: "Corrected ProPublica URL, academic fallback chain, and EDGAR User-Agent"
      contains: "ein_no_dashes"
    - path: ".claude/commands/research/init/SKILL.md"
      provides: "CLAUDE.md template with research-type field"
      contains: "research-type:"
  key_links:
    - from: ".claude/commands/research/discover/SKILL.md"
      to: ".claude/reference/discovery/channel-playbooks/regulatory.md"
      via: "ProPublica URL format and EDGAR User-Agent value"
      pattern: "ein_no_dashes"
    - from: ".claude/commands/research/discover/SKILL.md"
      to: ".claude/reference/discovery/channel-playbooks/academic.md"
      via: "Fallback chain matches section 6"
      pattern: "tavily_search.*OpenAlex fallback"
    - from: ".claude/commands/research/init/SKILL.md"
      to: ".claude/commands/research/discover/SKILL.md"
      via: "research-type field written by init, read by discover pre-check step 3"
      pattern: "research-type:"
---

<objective>
Fix all 4 cross-phase documentation inconsistencies identified in the v1.1 milestone audit across the discover and init SKILL.md files.

Purpose: Eliminate integration gaps where the discover skill's instructions contradict the channel playbooks it references, and where the init skill fails to write a field the discover skill expects to read.
Output: Corrected SKILL.md files for both discover and init skills.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md

@.claude/reference/discovery/channel-playbooks/regulatory.md
@.claude/reference/discovery/channel-playbooks/academic.md
</context>

<interfaces>
<!-- The executor needs these exact values from the playbooks to make corrections -->

From .claude/reference/discovery/channel-playbooks/regulatory.md:
- User-Agent header value (line 28): `User-Agent: ResearchAgent (contact@example.com)`
- ProPublica URL format (line 225): `https://projects.propublica.org/nonprofits/organizations/{ein_no_dashes}` — replace dashes in EIN with nothing (e.g., `13-1837418` -> `131837418`)

From .claude/reference/discovery/channel-playbooks/academic.md section 6 (Degradation Behavior):
- Fallback chain is: OpenAlex -> tavily_search with academic domain scoping -> WebSearch with academic keywords
- There is NO Semantic Scholar step in the fallback chain
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix 3 inconsistencies in discover SKILL.md</name>
  <files>.claude/commands/research/discover/SKILL.md</files>
  <action>
Make three targeted edits to `.claude/commands/research/discover/SKILL.md`:

**Fix 1 — ProPublica EIN format (CHAN-04):**
Line 299 currently reads:
`- Construct 990 profile URL: https://projects.propublica.org/nonprofits/organizations/{ein}`
Change `{ein}` to `{ein_no_dashes}` and add the dash-removal note. The corrected line should read:
`- Construct 990 profile URL: https://projects.propublica.org/nonprofits/organizations/{ein_no_dashes}` — remove dashes from EIN (e.g., `13-1837418` → `131837418`)

Also check line 300 (the PDF link URL) — it currently uses `{ein}` too:
`- PDF link for individual 990: https://projects.propublica.org/nonprofits/organizations/{ein}/{filing_id}/full`
Change to use `{ein_no_dashes}` as well:
`- PDF link for individual 990: https://projects.propublica.org/nonprofits/organizations/{ein_no_dashes}/{filing_id}/full`

**Fix 2 — Academic fallback chain (CHAN-02):**
Lines 279-284 currently list a 3-step fallback chain:
1. Semantic Scholar API (Bash curl) — if OpenAlex fails
2. tavily_search with academic domain scoping
3. WebSearch with site: operators

Replace with the 2-step chain from academic.md section 6:
1. `tavily_search` with academic domain scoping (scholar.google.com, arxiv.org, pubmed.ncbi.nlm.nih.gov) — label: `[via Tavily fallback]`
2. `WebSearch` with academic domain keywords — label: `[via WebSearch fallback]`

Remove the Semantic Scholar fallback step entirely. Remove the `[via Semantic Scholar fallback]` label from line 284.

**Fix 3 — EDGAR User-Agent value (CHAN-03):**
Line 293 currently reads:
`- Always include User-Agent: Research Agent research-agent@example.com header`
The value in regulatory.md (line 28) is: `User-Agent: ResearchAgent (contact@example.com)`

Two options to resolve: either update the hardcoded value to match regulatory.md, or remove the "copy exactly" language from Guardrail 4. The cleaner fix is to update the value AND keep the guardrail pointing to regulatory.md as the source of truth.

Change line 293 to:
`- Always include User-Agent: ResearchAgent (contact@example.com) header — required by SEC policy`

Also update Guardrail 4 (line 315) — it currently says:
`Always include User-Agent: Research Agent research-agent@example.com for SEC EDGAR requests.`
Change to:
`Always include User-Agent: ResearchAgent (contact@example.com) for SEC EDGAR requests. This is required by SEC policy — the header value is defined in regulatory.md.`

Also check the Common Failure Modes table (line 332) for any matching User-Agent reference and update it similarly.
  </action>
  <verify>
    <automated>grep -n "ein_no_dashes" .claude/commands/research/discover/SKILL.md && grep -n "Semantic Scholar" .claude/commands/research/discover/SKILL.md; echo "EXIT:$?" && grep -n "ResearchAgent (contact@example.com)" .claude/commands/research/discover/SKILL.md</automated>
  </verify>
  <done>
1. All ProPublica URLs in discover SKILL.md use `{ein_no_dashes}` (grep confirms presence)
2. Zero occurrences of "Semantic Scholar" remain in discover SKILL.md (grep returns no matches)
3. EDGAR User-Agent value matches regulatory.md: `ResearchAgent (contact@example.com)` (grep confirms)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add research-type field to init CLAUDE.md template</name>
  <files>.claude/commands/research/init/SKILL.md</files>
  <action>
In `.claude/commands/research/init/SKILL.md`, Step 4 "Assemble and Write Files" section, subsection "CLAUDE.md" — add a machine-readable `research-type:` field to the CLAUDE.md template.

The field must be:
- At the top of the CLAUDE.md template, before the "Project Purpose" paragraph
- Machine-readable (parseable by discover skill's pre-check step 3 which does: "Find the `research-type` field")
- Using the exact research type value from the user's Question 1 answer

Add this block at the very beginning of the CLAUDE.md assembly instructions (before item "1. Project Purpose"), as a new item 0 or as a frontmatter-style header:

```
research-type: {research-type}
```

Where `{research-type}` is the kebab-case research type selected by the user (e.g., `company-for-profit`, `company-non-profit`, `market-industry`, `prd-validation`, `competitive-analysis`, `person-research`, `exploratory-thesis`, `curriculum-research`, `presentation-research`).

The simplest approach: add a line immediately after the "Assemble a slim CLAUDE.md by combining:" instruction that says:

"0. **Research Type Field** — A single line at the top of the file: `research-type: {type}` where `{type}` is the kebab-case research type value from the user's Question 1 answer (e.g., `company-for-profit`, `market-industry`). This field is machine-readable — the discover skill's pre-check reads it to select the correct type-channel map."

This ensures the discover skill's pre-check step 3 ("Read the project's CLAUDE.md. Find the `research-type` field.") will find a parseable value.
  </action>
  <verify>
    <automated>grep -n "research-type:" .claude/commands/research/init/SKILL.md | head -5</automated>
  </verify>
  <done>
The init SKILL.md Step 4 CLAUDE.md assembly instructions include a `research-type:` field directive that will produce a machine-readable line in scaffolded projects' CLAUDE.md files, matching what discover SKILL.md pre-check step 3 expects to parse.
  </done>
</task>

</tasks>

<verification>
Run all verification checks to confirm cross-phase consistency:

1. ProPublica URL uses correct EIN format:
   `grep "ein_no_dashes" .claude/commands/research/discover/SKILL.md` — should match
   `grep "{ein}" .claude/commands/research/discover/SKILL.md` — should return zero matches (only `{ein_no_dashes}` variants)

2. No phantom Semantic Scholar fallback:
   `grep -c "Semantic Scholar" .claude/commands/research/discover/SKILL.md` — should return 0

3. EDGAR User-Agent matches regulatory.md:
   `grep "ResearchAgent (contact@example.com)" .claude/commands/research/discover/SKILL.md` — should match
   `grep "Research Agent research-agent@example.com" .claude/commands/research/discover/SKILL.md` — should return zero matches

4. Init produces research-type field:
   `grep "research-type:" .claude/commands/research/init/SKILL.md` — should match
</verification>

<success_criteria>
All four integration inconsistencies from the v1.1 milestone audit are resolved:
- ProPublica URLs use `{ein_no_dashes}` per regulatory.md
- Academic fallback chain matches academic.md section 6 (no Semantic Scholar)
- EDGAR User-Agent matches regulatory.md value
- Init CLAUDE.md template includes machine-readable `research-type:` field for discover pre-check
</success_criteria>

<output>
After completion, create `.planning/phases/06-discover-skill-consistency-fixes/06-01-SUMMARY.md`
</output>
