---
phase: 14-web-channel-diversity
plan: "02"
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - .claude/commands/research/discover/SKILL.md
autonomous: true
requirements:
  - DISC-03
  - DISC-04

must_haves:
  truths:
    - "Discover skill documents Exa as secondary tool in Web Search channel execution"
    - "Execution order (Tavily first, Exa second) is explicit in the skill"
    - "Combined status line format is documented in the skill"
    - "Exa degradation behavior is referenced from the skill"
  artifacts:
    - path: ".claude/commands/research/discover/SKILL.md"
      provides: "Web Search subsection extended with Exa parallel execution documentation"
      contains: "Exa"
  key_links:
    - from: "discover/SKILL.md Web Search subsection"
      to: "web-search.md Section 8"
      via: "references playbook dedup rules"
      pattern: "web-search.md"
---

<objective>
Update the discover skill's Web Search channel execution subsection to document Exa as a parallel secondary tool alongside Tavily, including execution order, authentication, combined status line, and degradation reference.

Purpose: Ensures the discover skill orchestrator knows to execute Exa after Tavily and report combined results. Completes the DISC-03/DISC-04 integration at the skill level.
Output: Updated discover/SKILL.md Web Search subsection with dual-tool execution documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-web-channel-diversity/14-CONTEXT.md
@.planning/phases/14-web-channel-diversity/14-PATTERNS.md
@.planning/phases/14-web-channel-diversity/14-01-SUMMARY.md

<interfaces>
<!-- Current Web Search subsection in discover/SKILL.md (lines 314-324) -->
From .claude/commands/research/discover/SKILL.md:
```markdown
### Web Search (channel-type: web-search)

Primary tool: `tvly search` (via Bash)

Execution parameters (from web-search.md playbook):
- `--topic general` for background/overview phases
- `--topic news --time-range month` for recent-developments phases
- `--include-domains`: leave empty for general web search (returns broadest results)
- `--exclude-domains`: exclude low-credibility aggregators if specified in playbook

Fallback chain: `npx firecrawl-cli search` (label: `[Firecrawl fallback]`) → `WebSearch` (label: `[WebSearch fallback]`)
```

<!-- Academic subsection pattern (lines 382-396) showing multi-API documentation -->
From .claude/commands/research/discover/SKILL.md:
```markdown
### Academic (channel-type: academic)

Primary: `Bash` curl to OpenAlex API

Key parameters:
- Include `mailto=research-agent@example.com` in all OpenAlex requests (activates 10 req/s polite pool)
- Extract per result: title, DOI, authors, citation count, open-access status (`is_oa`), OA URL (`open_access.oa_url`)
- Status determination: `is_oa=true` → ACCESSIBLE, `is_oa=false` → DISCOVERED

Rate limit: 10 req/s with mailto, 1 req/s without.

Fallback chain (per academic.md):
1. `tvly search --include-domains "scholar.google.com,arxiv.org,pubmed.ncbi.nlm.nih.gov"` — label: `[via tvly fallback]`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend discover/SKILL.md Web Search subsection with Exa parallel execution</name>
  <files>.claude/commands/research/discover/SKILL.md</files>
  <read_first>
    - .claude/commands/research/discover/SKILL.md
    - .claude/reference/discovery/channel-playbooks/web-search.md
    - .planning/phases/14-web-channel-diversity/14-CONTEXT.md
  </read_first>
  <action>
Locate the Web Search subsection in discover/SKILL.md (currently lines 314-324, starting with `### Web Search (channel-type: web-search)`). Replace the entire subsection content (from the `### Web Search` heading through the `Fallback chain:` line, stopping before the next `###` heading) with the following expanded version:

```markdown
### Web Search (channel-type: web-search)

Primary tool: `tvly search` (via Bash)
Secondary tool: Exa Search API (via Bash curl to `https://api.exa.ai/search`)

**Execution order:** Tavily first (up to 8 results), then Exa second (up to 8 results). Dedup collapses URL matches before presenting combined candidates. Maximum pre-dedup output: 16 candidates from web search.

**Tavily execution parameters** (from web-search.md playbook):
- `--topic general` for background/overview phases
- `--topic news --time-range month` for recent-developments phases
- `--include-domains`: leave empty for general web search (returns broadest results)
- `--exclude-domains`: exclude low-credibility aggregators if specified in playbook

**Exa execution parameters** (from web-search.md playbook):
- Authentication: `EXA_API_KEY` environment variable (free tier: 1,000 searches/month)
- Search type: `auto` (routes to neural or keyword via Exa's internal categorization)
- `numResults`: 8
- `useAutoprompt`: true (lets Exa refine the query for semantic relevance)
- Optional: `startPublishedDate` for time-bounded queries, `category` for entity-type biasing

**Cross-tool deduplication** (per web-search.md Section 8):
- Dedup key: exact URL string equality (no normalization)
- Priority: Tavily entry kept; Exa duplicate silently dropped
- Surviving duplicate tagged `[Tavily+Exa]`; Tavily-only tagged `[Tavily]`; Exa-only tagged `[Exa]`

**Combined status line** (report after both tools complete):
```
Web Search: {N} results (Tavily: {n1}, Exa-only: {n2}, deduped: {n3}) [degraded: {list}]
```

**Tavily fallback chain** (unchanged): `npx firecrawl-cli search` (label: `[Firecrawl fallback]`) → `WebSearch` (label: `[WebSearch fallback]`)

**Exa degradation:** If `EXA_API_KEY` is absent, or Exa returns error/timeout, skip silently. Log: `Exa: unavailable — web search results from Tavily only`. Exa failure does not trigger Tavily fallbacks — they are independent parallel tools.
```

This replaces lines 314-324. Do NOT modify any other subsection (Financial, Social Signals, Academic, etc.). The next `###` heading after Web Search must remain in place unchanged.
  </action>
  <verify>
    <automated>grep -c "Exa" .claude/commands/research/discover/SKILL.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - discover/SKILL.md contains "Secondary tool: Exa Search API" in the Web Search subsection
    - discover/SKILL.md contains "Execution order:" with "Tavily first" and "Exa second"
    - discover/SKILL.md contains "EXA_API_KEY" environment variable reference
    - discover/SKILL.md contains "Cross-tool deduplication" heading
    - discover/SKILL.md contains the status line format "Web Search: {N} results (Tavily: {n1}, Exa-only: {n2}, deduped: {n3})"
    - discover/SKILL.md contains "Exa degradation:" paragraph with "skip silently" behavior
    - discover/SKILL.md still contains "Tavily fallback chain" referencing Firecrawl and WebSearch
    - The Financial subsection heading ("### Financial") still exists unchanged after the Web Search subsection
    - grep -c "### Web Search" returns exactly 1 (no duplication)
  </acceptance_criteria>
  <done>discover/SKILL.md Web Search subsection is extended with Exa as secondary tool, execution order documented, dedup rules referenced, combined status line specified, and independent degradation paths documented. All other subsections unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan modifies orchestration documentation only; no new trust boundaries beyond those established in Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | EXA_API_KEY reference in SKILL.md | accept | Documentation only; actual key is in env var, not in file content. Same pattern as TAVILY_API_KEY already documented in this file. |
</threat_model>

<verification>
After plan completion:
1. `grep "Secondary tool: Exa" .claude/commands/research/discover/SKILL.md` returns a match
2. `grep "Execution order:" .claude/commands/research/discover/SKILL.md` returns a match
3. `grep "Cross-tool deduplication" .claude/commands/research/discover/SKILL.md` returns a match
4. `grep "Web Search:.*Tavily:.*Exa-only:.*deduped:" .claude/commands/research/discover/SKILL.md` returns a match
5. `grep "### Financial" .claude/commands/research/discover/SKILL.md` still returns a match (no collateral damage)
</verification>

<success_criteria>
discover/SKILL.md's Web Search channel execution subsection fully documents the dual-tool (Tavily + Exa) execution pattern, dedup contract, combined status line, and independent degradation — completing the skill-level documentation for DISC-03 and DISC-04.
</success_criteria>

<output>
After completion, create `.planning/phases/14-web-channel-diversity/14-02-SUMMARY.md`
</output>
