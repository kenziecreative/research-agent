---
phase: 05-tools-guide-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/reference/tools-guide.md
autonomous: true
requirements:
  - INIT-03

must_haves:
  truths:
    - "Guide documents when to use tavily_search (discovery) vs tavily_extract (processing) with a clear 'search first, extract after' rule"
    - "Guide includes a channel-tool mapping table covering all 6 channels with primary tool, fallback, and key parameter hints"
    - "Guide covers non-Tavily tools (Bash curl, WebSearch, URL construction) with usage-pattern guidance"
    - "Guide includes a common mistakes section with 3-5 anti-patterns"
  artifacts:
    - path: ".claude/reference/tools-guide.md"
      provides: "Discovery-aware tools reference guide"
      contains: "search first, extract after"
  key_links:
    - from: ".claude/reference/tools-guide.md"
      to: ".claude/reference/discovery/channel-playbooks/"
      via: "pointer references in channel-tool table"
      pattern: "channel-playbooks"
---

<objective>
Update the tools guide with discovery-specific patterns and channel-specific tool recommendations.

Purpose: Users and agents need a single quick-reference to know when to use search vs. extract and which tools to reach for per discovery channel. The current guide covers only Tavily basics (21 lines) and predates the v1.1 discovery infrastructure.

Output: Expanded `.claude/reference/tools-guide.md` covering the full discovery-to-processing tool landscape.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tools-guide-update/05-CONTEXT.md
@.claude/reference/tools-guide.md
@.claude/reference/discovery/channel-playbooks/web-search.md
@.claude/reference/discovery/channel-playbooks/academic.md
@.claude/reference/discovery/channel-playbooks/regulatory.md
@.claude/reference/discovery/channel-playbooks/financial.md
@.claude/reference/discovery/channel-playbooks/social-signals.md
@.claude/reference/discovery/channel-playbooks/domain-specific.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand tools guide with discovery sections</name>
  <files>.claude/reference/tools-guide.md</files>
  <action>
Rewrite `.claude/reference/tools-guide.md` in place. Preserve the existing content's intent but expand significantly. Follow the structure decided in 05-CONTEXT.md:

1. **Title**: Change to "Tools Guide — Research Discovery and Processing"

2. **Tool Reference table** (update existing): Keep the current 4 Tavily tools. Add a row for `WebSearch` (general web fallback when Tavily unavailable, less targeted, label results as degraded). Add a row for `Bash curl` (direct API access for OpenAlex academic and EDGAR/ProPublica regulatory, returns structured JSON). Add a row for URL construction (build search URLs like Google Patents for extraction via tavily_extract). Keep the table format: Tool | Use For | Don't Use For.

3. **Search vs. Extract Workflow** (new section, place after tool reference): Lead with the hard rule: "Search first, extract after. Never extract a URL you haven't seen in search results or a candidates file — that's skipping evaluation." Explain `tavily_search` = Discovery (find sources) and `tavily_extract` = Processing (read sources). Add a brief note distinguishing automated discovery (`/research:discover` via playbooks) from manual `tavily_search` (targeted follow-up, one-off lookups, projects not using discover). Briefly mention DISCOVERED / ACCESSIBLE / PROCESSED status taxonomy with a pointer to channel playbooks for the full definition.

4. **Channel-Tool Mapping** (new section): Create a single table with columns: Channel | Primary Tool | Fallback | Key Param. Rows:
   - web-search | `tavily_search` | `WebSearch` | `include_domains`, `search_depth`
   - academic | `Bash curl` (OpenAlex API) | `tavily_search` with academic domains | `mailto` param for polite pool
   - regulatory | `Bash curl` (EDGAR/ProPublica) | `tavily_search` with SEC domains | `User-Agent` header required
   - financial | `tavily_search` | `WebSearch` | `include_domains` for financial sites
   - social-signals | `tavily_search` | `WebSearch` with site: operators | `topic: "news"`, `include_domains`
   - domain-specific | Varies (see playbook) | `tavily_search` / `WebSearch` | Type hooks per research type

   After table, add pointer: "For full query templates and parameters, see `.claude/reference/discovery/channel-playbooks/`."
   Add brief degradation summary: primary fails leads to fallback automatically, results labeled with source method, all-fail leads to empty candidates with status report.

5. **Search Patterns** (keep existing section): Retain existing content about search_depth basic vs advanced. Light edit for coherence if needed.

6. **Crawl Discipline** (keep existing section): Retain existing content. Light edit for coherence if needed.

7. **Common Mistakes** (new section, at end): Short list of 3-5 anti-patterns. Cover these themes:
   - Extracting before searching (skipping evaluation)
   - Using WebSearch when Tavily is available (less targeted)
   - Crawling a domain without mapping first
   - Running manual tavily_search for systematic discovery when /research:discover exists
   - Extracting snippet URLs instead of actual source URLs

Keep the guide concise — use tables and bullets, not paragraphs. Point to playbooks for details rather than duplicating content. Target roughly 80-120 lines total.
  </action>
  <verify>
    <automated>wc -l .claude/reference/tools-guide.md | awk '{if ($1 >= 60 && $1 <= 150) print "PASS: " $1 " lines"; else print "FAIL: " $1 " lines (expected 60-150)"}'</automated>
  </verify>
  <done>Tools guide contains: updated title, expanded tool reference table (6+ tools), search-vs-extract workflow rule, channel-tool mapping table (6 channels), common mistakes section (3-5 items), and pointers to channel playbooks</done>
</task>

<task type="auto">
  <name>Task 2: Verify guide content and cross-references</name>
  <files>.claude/reference/tools-guide.md</files>
  <action>
Validate the updated tools guide against INIT-03 requirements and CONTEXT.md decisions:

1. Verify the guide contains "search first, extract after" or equivalent clear workflow rule
2. Verify all 6 channels appear in the channel-tool mapping table (web-search, academic, regulatory, financial, social-signals, domain-specific)
3. Verify pointer to `.claude/reference/discovery/channel-playbooks/` exists
4. Verify WebSearch appears in the tool reference table
5. Verify common mistakes section exists with at least 3 items
6. Verify no content duplicates full query syntax from playbooks (guide should be concise pointers)
7. Verify the title has been updated from "Tavily MCP" to include discovery/processing language

If any check fails, fix the guide content in place.
  </action>
  <verify>
    <automated>grep -c "search first" .claude/reference/tools-guide.md && grep -c "channel-playbooks" .claude/reference/tools-guide.md && grep -c "WebSearch" .claude/reference/tools-guide.md && grep -c "Common Mistakes\|Common mistakes\|Anti-Patterns\|Anti-patterns" .claude/reference/tools-guide.md | awk '{if ($1 >= 1) print "PASS"; else print "FAIL: missing required section"}'</automated>
  </verify>
  <done>Guide passes all content checks: workflow rule present, all 6 channels mapped, playbook pointers included, WebSearch documented, common mistakes section exists, title updated</done>
</task>

</tasks>

<verification>
Content checks:
- `grep -i "search first" .claude/reference/tools-guide.md` returns match
- `grep -c "web-search\|academic\|regulatory\|financial\|social-signals\|domain-specific" .claude/reference/tools-guide.md` returns 6+
- `grep "channel-playbooks" .claude/reference/tools-guide.md` returns match
- File length between 60-150 lines (concise, not bloated)
</verification>

<success_criteria>
1. Tools guide documents when to use tavily_search (discovery) vs tavily_extract (processing) with the "search first, extract after" rule
2. Tools guide includes channel-specific tool recommendations for all 6 discovery channels in a single mapping table
3. Tools guide covers non-Tavily tools (WebSearch, Bash curl, URL construction)
4. Tools guide includes 3-5 common mistake anti-patterns
5. Tools guide points to channel playbooks for full details rather than duplicating content
</success_criteria>

<output>
After completion, create `.planning/phases/05-tools-guide-update/05-01-SUMMARY.md`
</output>
