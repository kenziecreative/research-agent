---
phase: 11-claim-graph-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/research/audit-claims/SKILL.md
autonomous: true
requirements:
  - TRACE-01

must_haves:
  truths:
    - "audit-claims writes a claim node for every factual claim it traces, regardless of pass/fail outcome"
    - "Each claim node carries edges to source files and canonical figure IDs"
    - "Graph write failures do not block audit promotion"
    - "Re-auditing the same draft overwrites existing claim nodes instead of duplicating them"
  artifacts:
    - path: ".claude/commands/research/audit-claims/SKILL.md"
      provides: "Step 8b — claim graph node write logic"
      contains: "Write claim graph nodes"
  key_links:
    - from: ".claude/commands/research/audit-claims/SKILL.md"
      to: "research/reference/claim-graph.json"
      via: "step 8b reads then writes claim-graph.json"
      pattern: "claim-graph\\.json"
---

<objective>
Add step 8b to audit-claims SKILL.md that writes claim graph nodes to research/reference/claim-graph.json after confidence tier computation.

Purpose: This is the core write path for the claim graph. Every factual claim traced during an audit becomes a node with typed edges to source notes and canonical figures, fulfilling TRACE-01.

Output: Modified audit-claims/SKILL.md with step 8b inserted between step 8a and step 9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-claim-graph-foundation/11-CONTEXT.md
@.planning/phases/11-claim-graph-foundation/11-PATTERNS.md

<interfaces>
<!-- Existing audit-claims step structure the executor must understand -->

From .claude/commands/research/audit-claims/SKILL.md:
- Step 5: traces every factual claim to source notes (produces claim text + source_files)
- Step 6a: reads canonical-figures.json (produces figure ID matches)
- Step 8a (lines 55-77): computes per-section confidence tier, evidence_directness, source_count
- Step 9 (line 79): writes audit report to research/audits/
- Pass/Fail Criteria section follows step 9

The new step 8b inserts AFTER step 8a (line 77) and BEFORE step 9 (line 79).

From .claude/reference/templates/canonical-figures.json (the pattern):
```json
{
  "figures": []
}
```

Claim-graph.json follows this same shell with key "claims" instead of "figures".

Claim node schema (per D-05):
```json
{
  "id": "c001-market-size-exceeds-four",
  "text": "The claim text as extracted from the draft.",
  "phase": 1,
  "section": "Section Name",
  "confidence_tier": "High|Moderate|Low|Insufficient",
  "source_files": ["note-filename-1.md", "note-filename-2.md"],
  "figure_ids": ["figure-slug-1"],
  "evidence_directness": "Direct|Indirect",
  "source_count": 3
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert step 8b into audit-claims SKILL.md</name>
  <files>.claude/commands/research/audit-claims/SKILL.md</files>
  <read_first>
    - .claude/commands/research/audit-claims/SKILL.md (full file — understand step ordering, line numbers, existing patterns)
    - .planning/phases/11-claim-graph-foundation/11-PATTERNS.md (lines 56-90 — the exact step 8b text and positioning rules)
    - .planning/phases/11-claim-graph-foundation/11-CONTEXT.md (D-03 through D-06 — recording timing, schema, error handling decisions)
  </read_first>
  <action>
Insert a new step 8b into audit-claims/SKILL.md between step 8a (ends at line 77, after "Add to the scorecard output (step 8) after severity distribution.") and step 9 (line 79, "Write audit report to...").

The step 8b text to insert (verbatim — this is markdown, not code):

```
8b. **Write claim graph nodes to `research/reference/claim-graph.json`.**

   For every factual claim traced in step 5, construct a claim node using the data already in context:
   - `id` — sequential prefix (c001, c002, ...) + slug from first 4-5 words of claim text (lowercase, hyphenated, non-alphanumeric stripped). Example: `"c001-market-size-exceeds-four"`. Sequential prefix guarantees uniqueness; slug makes IDs human-scannable.
   - `text` — the claim text as traced in step 5
   - `phase` — current phase number (read from `research/STATE.md` `Active phase` field)
   - `section` — section name from the audit pass
   - `confidence_tier` — tier computed in step 8a for this claim's section (High / Moderate / Low / Insufficient)
   - `source_files` — array of note filenames traced in step 5
   - `figure_ids` — array of figure IDs from canonical-figures.json that appear in this claim (empty array `[]` if none)
   - `evidence_directness` — Direct / Indirect classification from step 8a
   - `source_count` — integer count of independent sources from step 8a

   Read `research/reference/claim-graph.json`. If the file does not exist, create it with `{"claims": []}`. If it exists but fails to parse as JSON, log a warning in the audit report and skip the graph write — do not fail the audit.

   For claims already present in the graph (matched by `phase` + `section` + `text` equality), overwrite the existing node with the new data. For new claims, append to the `claims` array.

   Write the updated JSON back to `research/reference/claim-graph.json`.

   **After writing, verify the write succeeded.** Re-read the file and confirm it parses as valid JSON with a `claims` array. If the read fails or the array is missing, log: "WARNING: claim-graph.json write failed — graph incomplete for this phase. Re-run `/research:audit-claims` to retry graph write without re-running the full audit." Do not fail the audit or block promotion.
```

After inserting step 8b, also add a new entry to the "Common Failure Modes" table at the end of the file. Insert as the last row before the closing `|`:

```
| Graph write blocking audit promotion — treating a claim-graph.json write failure as an audit failure | Graph write is supplementary infrastructure, not an evidence gate. If claim-graph.json cannot be written or parsed, log a WARNING in the audit report but continue to the pass/fail determination. The audit gate protects research quality; the graph is for downstream traceability (Phase 12). Never fail an audit or block promotion due to a graph write issue. |
```
  </action>
  <verify>
    <automated>grep -n "8b\. \*\*Write claim graph nodes" .claude/commands/research/audit-claims/SKILL.md && grep -n "claim-graph.json" .claude/commands/research/audit-claims/SKILL.md | head -10 && grep -n "Graph write blocking audit promotion" .claude/commands/research/audit-claims/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/research/audit-claims/SKILL.md contains `8b. **Write claim graph nodes to `research/reference/claim-graph.json`.**`
    - Step 8b appears AFTER the line containing "Add to the scorecard output (step 8) after severity distribution." (end of step 8a)
    - Step 8b appears BEFORE the line containing "9. **Write audit report" (start of step 9)
    - Step 8b contains all 9 field definitions: `id`, `text`, `phase`, `section`, `confidence_tier`, `source_files`, `figure_ids`, `evidence_directness`, `source_count`
    - Step 8b contains the deduplication rule: `matched by `phase` + `section` + `text` equality`
    - Step 8b contains the error handling pattern: `do not fail the audit`
    - Step 8b contains the ID generation example: `c001-market-size-exceeds-four`
    - Step 8b contains the verify-after-write pattern: `Re-read the file and confirm it parses as valid JSON`
    - Common Failure Modes table contains `Graph write blocking audit promotion`
  </acceptance_criteria>
  <done>audit-claims/SKILL.md has a complete step 8b that writes claim graph nodes with all 9 schema fields, deduplication by phase+section+text, non-blocking error handling, and a corresponding failure mode entry</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-claims -> claim-graph.json | Agent writes structured data to a project-local JSON file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | claim-graph.json | accept | File is project-local, write-once per audit run, agent-only access. No user-facing security boundary — this is an internal data structure within a single-user research tool. |
| T-11-02 | Denial of Service | claim-graph.json parse failure | mitigate | Step 8b explicitly handles parse failures by logging a warning and skipping the graph write — audit continues unblocked. |
</threat_model>

<verification>
1. `grep -c "8b\." .claude/commands/research/audit-claims/SKILL.md` returns 1
2. Step ordering is 8a -> 8b -> 9 (verified by line number ordering in grep output)
3. All 9 claim node fields are present in step 8b text
4. Error handling is non-blocking (contains "do not fail the audit")
</verification>

<success_criteria>
audit-claims/SKILL.md contains a complete step 8b that instructs the agent to write claim graph nodes with the full D-05 schema, deduplication logic, and non-blocking error handling, positioned between confidence tier computation and audit report writing.
</success_criteria>

<output>
After completion, create `.planning/phases/11-claim-graph-foundation/11-01-SUMMARY.md`
</output>
