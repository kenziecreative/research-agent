---
phase: 12-claim-graph-operations
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - .claude/agents/research-integrity.md
autonomous: true
requirements: [TRACE-02]

must_haves:
  truths:
    - "When claim-graph.json contains nodes with drift_warning fields, the research-integrity agent surfaces them during check 9"
    - "Drift warnings use the ALL_CAPS_PREFIX flag format: DRIFT WARNING ACTIVE:"
    - "Drift warnings are advisory — they do not block promotion"
    - "The surface message includes figure_id, expected_value, and canonical_value from the drift_warning object"
  artifacts:
    - path: ".claude/agents/research-integrity.md"
      provides: "Drift warning surfacing in integrity check 9"
      contains: "DRIFT WARNING ACTIVE"
  key_links:
    - from: "research-integrity check 9"
      to: "claim-graph.json drift_warning field"
      via: "reads nodes and surfaces warnings"
      pattern: "drift_warning"
---

<objective>
Extend research-integrity agent check 9 to surface active drift_warning fields from claim-graph.json, making drift visible outside of audit-claims runs.

Purpose: Completes TRACE-02 by ensuring drift warnings are not silently stored — they surface whenever the integrity agent reviews a file, prompting the user to re-audit.

Output: Modified `.claude/agents/research-integrity.md` with drift_warning surface extension appended to check 9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-claim-graph-operations/12-CONTEXT.md
@.planning/phases/12-claim-graph-operations/12-PATTERNS.md
@.planning/phases/12-claim-graph-operations/12-01-SUMMARY.md

<interfaces>
<!-- From research-integrity.md check 9 (current state): -->

Check 9 currently:
- Applies when claim-graph.json exists and file under review is phase output or draft
- Reads claim-graph.json, matches nodes by phase
- Compares confidence_tier in graph vs audit report
- Flags with "CLAIM GRAPH INCONSISTENCY:" prefix
- Advisory — does not block promotion

<!-- drift_warning field schema (set by audit-claims step 6a per Plan 01): -->

drift_warning object on a claim node:
{
  "figure_id": "<id from figure_ids>",
  "expected_value": "<value stored in claim at last audit>",
  "canonical_value": "<current registry value>"
}

<!-- Integrity check flag format (from check 5): -->
Pattern: "ALL_CAPS_PREFIX: [file/claim reference] [what was found] [what was expected] [what needs to change]"
Example: "CROSS-PHASE DRIFT: Line [N] says [value], but canonical-figures.json says [different value]"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend check 9 to surface drift_warning fields</name>
  <files>.claude/agents/research-integrity.md</files>
  <read_first>
    - .claude/agents/research-integrity.md
    - .planning/phases/12-claim-graph-operations/12-PATTERNS.md
  </read_first>
  <action>
In `.claude/agents/research-integrity.md`, locate check 9 (### 9. Claim Graph Consistency). After the existing final line of check 9 which reads `- This check is advisory — it surfaces drift between the graph and the audit record, but does not block promotion.`, append the following text:

```markdown
- Additionally, for any claim node with a `drift_warning` field, surface the warning:
  "DRIFT WARNING ACTIVE: claim [id] references figure [figure_id]. Expected value: [expected_value], current canonical value: [canonical_value]. The claim has not been re-audited since this figure changed. Run `/research:audit-claims` on the relevant draft to clear or confirm this warning."
- Drift warnings are advisory — they do not block promotion. Surface them so the human is prompted to act; do not silently pass over nodes with a `drift_warning` field set.
```

This follows the ALL_CAPS_PREFIX flag format established by check 5 (CROSS-PHASE DRIFT:) and maintains the advisory-not-gate pattern already established in check 9.
  </action>
  <verify>
    <automated>grep -n "DRIFT WARNING ACTIVE" .claude/agents/research-integrity.md && grep -n "drift_warning" .claude/agents/research-integrity.md && grep -c "advisory" .claude/agents/research-integrity.md</automated>
  </verify>
  <acceptance_criteria>
    - research-integrity.md contains exact string `"DRIFT WARNING ACTIVE: claim [id] references figure [figure_id]. Expected value: [expected_value], current canonical value: [canonical_value].`
    - research-integrity.md contains `Run /research:audit-claims` in the drift warning message
    - research-integrity.md contains `do not silently pass over nodes with a drift_warning field set`
    - The new text appears AFTER the line `- This check is advisory — it surfaces drift between the graph and the audit record, but does not block promotion.`
    - grep -c "advisory" returns at least 3 (original check 9 advisory line + new "Drift warnings are advisory" line + any prior usage)
    - The new text does NOT appear inside check 8 or after the "## How to Use This Agent" section — it is within check 9's scope
  </acceptance_criteria>
  <done>Check 9 surfaces active drift_warning fields using the DRIFT WARNING ACTIVE: flag format, prompts user to run audit-claims, and explicitly states it does not block promotion</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claim-graph.json read | Agent reads project-local JSON to surface warnings; read-only, no write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | drift_warning values shown in flag output | accept | Both source values are project-internal (canonical-figures.json registry values); no PII or secrets involved |
| T-12-05 | Denial of Service | Many drift_warnings flooding output | accept | Advisory system; high volume of warnings indicates a real problem the user needs to address |
</threat_model>

<verification>
After task completes:
1. `grep "DRIFT WARNING ACTIVE" .claude/agents/research-integrity.md` confirms flag text present
2. `grep "drift_warning" .claude/agents/research-integrity.md` returns at least 2 lines (the field reference + "do not silently pass over")
3. `grep -n "### 9" .claude/agents/research-integrity.md` confirms check 9 heading intact
4. The drift warning text appears between check 9's heading and the "## How to Use This Agent" section
</verification>

<success_criteria>
- research-integrity agent check 9 surfaces active drift_warning fields on claim nodes during any integrity review
- The surface format uses ALL_CAPS_PREFIX consistent with other integrity checks (DRIFT WARNING ACTIVE:)
- The warning includes the actionable recommendation to run `/research:audit-claims`
- The extension is explicitly advisory (does not block promotion)
- TRACE-02 is fully satisfied: drift is detected in audit-claims (Plan 01) AND surfaced by the integrity agent (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/12-claim-graph-operations/12-02-SUMMARY.md`
</output>
