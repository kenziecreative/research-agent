---
phase: 16-cli-polish
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - .claude/commands/research/init/SKILL.md
  - .claude/commands/research/discover/SKILL.md
  - .claude/commands/research/process-source/SKILL.md
  - .claude/commands/research/cross-ref/SKILL.md
  - .claude/commands/research/check-gaps/SKILL.md
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04]

must_haves:
  truths:
    - "Every skill that lacked a next-action block now ends with a ▶ NEXT: block using the canonical format from prompt-templates.md"
    - "discover and cross-ref use progressive disclosure: summary dashboard above ---, detail below"
    - "process-source uses bold labels without H3 headings for its compact output"
    - "check-gaps has context-sensitive next-action blocks that recommend discover when gaps exist and summarize-section when coverage is adequate"
    - "cross-ref has context-sensitive next-action blocks that vary based on unresolved contradictions"
    - "All next-action blocks use unicode box-drawing character delimiters, not ASCII hyphens"
    - "No banned phrases from the CLI Tone Rules appear in any modified Output section"
  artifacts:
    - path: ".claude/commands/research/init/SKILL.md"
      provides: "Next-action block in Step 7 Report"
      contains: "▶ NEXT:"
    - path: ".claude/commands/research/discover/SKILL.md"
      provides: "Progressive disclosure output section and next-action block"
      contains: "▶ NEXT:"
    - path: ".claude/commands/research/process-source/SKILL.md"
      provides: "Bold-label output summary and next-action block"
      contains: "▶ NEXT:"
    - path: ".claude/commands/research/cross-ref/SKILL.md"
      provides: "Progressive disclosure dashboard and context-sensitive next-action block"
      contains: "▶ NEXT:"
    - path: ".claude/commands/research/check-gaps/SKILL.md"
      provides: "Context-sensitive next-action block"
      contains: "▶ NEXT:"
  key_links:
    - from: "all 5 modified SKILL.md files"
      to: ".claude/reference/prompt-templates.md"
      via: "CLI Tone Rules and transition prompt format"
      pattern: "prompt-templates.md"
---

<objective>
Add next-action blocks and progressive disclosure to the 5 skills that currently lack them: init, discover, process-source, cross-ref, and check-gaps. Each skill gets the `▶ NEXT:` block format from prompt-templates.md, with context-sensitive recommendations per D-08. discover and cross-ref also get progressive disclosure dashboards per D-13/D-15.

Purpose: Completes UX-01 (consistent formatting), UX-02 (next-action blocks), UX-03 (plain language), and UX-04 (progressive disclosure) across all 10 skills. After this plan, every skill presents output with the same structure.

Output: 5 modified SKILL.md files with consistent Output sections, next-action blocks, and progressive disclosure where needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cli-polish/16-CONTEXT.md
@.planning/phases/16-cli-polish/16-PATTERNS.md
@.planning/phases/16-cli-polish/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add next-action block to init and restructure process-source Output section</name>
  <files>.claude/commands/research/init/SKILL.md, .claude/commands/research/process-source/SKILL.md</files>
  <read_first>
    - .claude/commands/research/init/SKILL.md (full file — the Step 7 section is at the end, around line 747+)
    - .claude/commands/research/process-source/SKILL.md (full file — the Output section is at lines 106-107)
    - .claude/reference/prompt-templates.md (updated in Plan 01 — read for tone rules and canonical format)
    - .planning/phases/16-cli-polish/16-PATTERNS.md (exact block content specified for both files)
    - .planning/phases/16-cli-polish/16-CONTEXT.md (D-04 for process-source short-output rule, D-05/D-06/D-07/D-08 for next-action blocks)
  </read_first>
  <action>
**File 1: init/SKILL.md — Add next-action block to Step 7**

Find the line in Step 7 that reads: `- Next steps: review the research plan in \`research/research-plan.md\`, then start Phase 1 with \`/research:discover\``

Replace that line (and any immediately following line about clearing context) with the following next-action block. The block goes at the very end of Step 7, after the phase table and the list of ten skills:

```markdown
───────────────────────────────────────────────────────────

**▶ NEXT:** `/research:discover` — Find candidate sources for Phase 1's questions using the type-channel map.

**Also available:**
- `/research:start-phase` — Get the full Phase 1 briefing before starting discovery.
- `/research:process-source <url-or-file>` — Process a specific source you already have.

**What to expect:** Discovery will surface a prioritized candidate list for Phase 1's channels based on the research type and questions just generated. After you approve, processing runs sequentially with a cross-reference checkpoint every 5-8 sources.

───────────────────────────────────────────────────────────
```

The `What to expect:` line is included per D-07 because the next step (discover) benefits from user context about what happens after init. The `Also available:` section lists start-phase and process-source as alternatives. Remove any prior prose about "Next steps" or "Reminder to clear context" that this block replaces — the block IS the next-steps guidance.

Keep the line about "Do NOT tell the user to `cd` anywhere" and "Do NOT initialize a git repo" — those are guardrails, not next-steps prose.

**File 2: process-source/SKILL.md — Replace Output section with structured format**

Replace the entire `## Output` section (currently lines 106-107, a single prose paragraph) with:

```markdown
## Output

**Source:** [title]
**Credibility:** [tier]
**Key findings:** [N] findings tagged for Phase [N]
**Contradictions:** [N found / none]
**Sources since last cross-ref:** [N]/5

If N >= 4: "Cross-reference is due — run `/research:cross-ref` after the next source."

───────────────────────────────────────────────────────────

**▶ NEXT:** `/research:process-source <next-url>` — Continue processing the approved candidate list.

**Also available:**
- `/research:cross-ref` — Run cross-reference now if 5+ sources have been processed.
- `/research:check-gaps` — Check coverage before processing more sources.

───────────────────────────────────────────────────────────
```

Per D-04: process-source is a short-output skill — bold labels without H3 sections. No H3 headings, no `---` progressive disclosure separator. Per D-07: `What to expect:` is omitted because the next step (continue processing or cross-ref) is unambiguous.

Note: The cross-ref reminder that was in the old prose ("If 'Sources since last cross-reference' is now 4 or 5, remind the user") is preserved as the `If N >= 4:` conditional line in the new format. Same behavior, structured presentation.
  </action>
  <verify>
    <automated>python3 -c "
init = open('.claude/commands/research/init/SKILL.md').read()
ps = open('.claude/commands/research/process-source/SKILL.md').read()
ok = True
# init checks
if '▶ NEXT:' not in init:
    print('FAIL: init missing next-action block')
    ok = False
if '/research:discover' not in init.split('▶ NEXT:')[1].split('\n')[0] if '▶ NEXT:' in init else '':
    print('FAIL: init next-action does not recommend discover')
    ok = False
if '───────────────────────────────────────────────────────────' not in init:
    print('FAIL: init missing unicode delimiters')
    ok = False
# process-source checks
if '▶ NEXT:' not in ps:
    print('FAIL: process-source missing next-action block')
    ok = False
if '**Source:**' not in ps:
    print('FAIL: process-source missing bold Source label')
    ok = False
if '**Credibility:**' not in ps:
    print('FAIL: process-source missing bold Credibility label')
    ok = False
if '**Key findings:**' not in ps:
    print('FAIL: process-source missing bold Key findings label')
    ok = False
if '### ' in ps.split('## Output')[1] if '## Output' in ps else '':
    print('FAIL: process-source Output has H3 heading (should be bold labels only per D-04)')
    ok = False
if ok:
    print('PASS: init and process-source both have correct structure')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - init/SKILL.md contains `▶ NEXT:` block at the end of Step 7 recommending `/research:discover`
    - init/SKILL.md contains `Also available:` listing `/research:start-phase` and `/research:process-source`
    - init/SKILL.md contains `What to expect:` line (per D-07, included because discover needs context)
    - init/SKILL.md contains unicode `───` delimiters above and below the block
    - init/SKILL.md no longer contains the old prose "Next steps: review the research plan" line
    - init/SKILL.md retains "Do NOT tell the user to `cd` anywhere" guardrail line
    - process-source/SKILL.md `## Output` section contains bold labels: `**Source:**`, `**Credibility:**`, `**Key findings:**`, `**Contradictions:**`, `**Sources since last cross-ref:**`
    - process-source/SKILL.md `## Output` section contains NO H3 headings (per D-04)
    - process-source/SKILL.md contains `▶ NEXT:` block recommending `/research:process-source <next-url>`
    - process-source/SKILL.md contains `Also available:` listing cross-ref and check-gaps
    - process-source/SKILL.md contains no `What to expect:` line (per D-07, omitted for unambiguous next step)
    - process-source/SKILL.md preserves the cross-ref reminder conditional ("If N >= 4")
  </acceptance_criteria>
  <done>init/SKILL.md has next-action block in Step 7 and process-source/SKILL.md has structured bold-label Output section with next-action block, both using canonical format from prompt-templates.md</done>
</task>

<task type="auto">
  <name>Task 2: Add progressive disclosure and next-action blocks to discover, cross-ref, and check-gaps</name>
  <files>.claude/commands/research/discover/SKILL.md, .claude/commands/research/cross-ref/SKILL.md, .claude/commands/research/check-gaps/SKILL.md</files>
  <read_first>
    - .claude/commands/research/discover/SKILL.md (full file — Output section is at lines 533-544)
    - .claude/commands/research/cross-ref/SKILL.md (full file — Output section is at lines 68-71)
    - .claude/commands/research/check-gaps/SKILL.md (full file — Output section is at lines 87-113)
    - .claude/reference/prompt-templates.md (updated in Plan 01 — read for canonical format and tone rules)
    - .planning/phases/16-cli-polish/16-PATTERNS.md (exact dashboard and block content for all three files)
    - .planning/phases/16-cli-polish/16-CONTEXT.md (D-08 context-sensitivity, D-13/D-14/D-15 progressive disclosure, D-05/D-06/D-07 next-action format)
  </read_first>
  <action>
**File 1: discover/SKILL.md — Add progressive disclosure to Output section + next-action block**

Replace the entire `## Output` section (lines 533-544) with:

```markdown
## Output

Print the completion summary (Step 7 format):

### Discovery: Phase [N] — [Phase Name]

| Channel | Status | Found |
|---------|--------|-------|
| Web Search | found | 6 |
| Academic | found | 4 |
| Regulatory | skipped | 0 |
| Financial | error (timeout) | 0 |

**Total candidates:** N | **Duplicates skipped:** N | Saved to: `research/discovery/{phase}-candidates.md`

---

[Per-channel candidate detail and prioritized list follow below]

After presenting the prioritized candidate list and receiving user approval to process, append the next-action block at the end of the discovery output (after the batch processing offer):

───────────────────────────────────────────────────────────

**▶ NEXT:** `/research:process-source <first-url>` — Start processing the approved candidates in priority order.

**Also available:**
- `/research:cross-ref` — Cross-reference sources processed so far before continuing.
- `/research:check-gaps` — Check coverage against the phase's questions before collecting more sources.

───────────────────────────────────────────────────────────

After discovery is complete and all approved sources have been processed (post-batch), render this block instead:

───────────────────────────────────────────────────────────

**▶ NEXT:** `/research:cross-ref` — Cross-reference the [N] sources processed so far before continuing.

**Also available:**
- `/research:check-gaps` — Check coverage against the phase's questions before collecting more sources.
- `/research:process-source <url>` — Process a specific source not in the candidates list.

───────────────────────────────────────────────────────────

`What to expect:` is omitted on both blocks per D-07 — the next steps after discover are unambiguous.

After discovery, the agent should guide the user through source processing — not hand back control and wait for manual URL entry. The user's job is to review and approve; the agent's job is to drive the workflow forward.

The candidates file at `research/discovery/{phase}-candidates.md` is the primary artifact of this skill.
```

Key points:
- The summary dashboard table (Channel/Status/Found) is the progressive disclosure summary per D-13/D-15.
- The `---` separator divides summary from per-channel detail (D-02).
- Two context-sensitive next-action blocks per D-08: one for post-discovery (start processing), one for post-batch (cross-ref).
- The existing synthesis-phase block at lines 39-49 is already correct and must NOT be changed.
- The existing Step 7 completion summary format and candidate presentation logic (lines 251-317) remain unchanged — they ARE the detail section below the `---` separator.

**File 2: cross-ref/SKILL.md — Add progressive disclosure dashboard to Output section + context-sensitive next-action block**

Replace the entire `## Output` section (lines 68-71) with:

```markdown
## Output

### Cross-Reference: Phase [N]

| Signal | Count |
|--------|-------|
| Contradictions (unresolved) | N |
| Contradictions (total) | N |
| Shared-origin clusters | N |
| Saturation advisory | triggered / not triggered |
| Patterns identified | N |

**Aggregate saturation:** [N%] confirmatory — [converging / still building]

---

[Contradictions detail, saturation per-question, cluster list, pattern list follow below]

Summarize new patterns found since the last cross-reference. Report: number of contradictions found (unresolved/total), saturation status (aggregate % and any per-question advisories), shared-origin clusters detected. Highlight any contradictions that block synthesis and any questions that are under-covered. If aggregate saturation advisory is triggered, suggest the user consider moving saturated questions to synthesis.

**Context-sensitive next-action block (per D-08):**

If unresolved contradictions exist:

───────────────────────────────────────────────────────────

**▶ NEXT:** Resolve the [N] unresolved contradiction(s) above — synthesis is blocked until core contradictions are confirmed.

**Also available:**
- `/research:check-gaps` — Check coverage after resolving contradictions.
- `/research:phase-insight` — Get a full picture of phase strength before deciding next steps.

───────────────────────────────────────────────────────────

If no blocking contradictions and coverage is converging:

───────────────────────────────────────────────────────────

**▶ NEXT:** `/research:check-gaps` — Confirm coverage for Phase [N] before synthesis.

**Also available:**
- `/research:process-source <url>` — Process additional sources if any questions remain under-covered.
- `/research:phase-insight` — Analyze phase strength in detail before deciding.

───────────────────────────────────────────────────────────

`What to expect:` is omitted on both paths per D-07 — next steps are unambiguous.
```

Key points:
- The signals dashboard table is the progressive disclosure summary per D-13/D-15.
- The `---` separator divides dashboard from detailed analysis (D-02).
- Two context-sensitive blocks per D-08: contradictions path vs. clean path.
- H3 heading `### Cross-Reference: Phase [N]` follows D-01 hierarchy (H2 for skill title, H3 for named sections).

**File 3: check-gaps/SKILL.md — Add context-sensitive next-action block to Output section**

The existing Output section (lines 87-113) already has progressive disclosure (dashboard + per-question detail). Append the following context-sensitive next-action blocks after the existing content (after line 113, the "and N more" line):

```markdown

**Context-sensitive next-action block:**

If gaps exist (Not Started, Lopsided, or Evidence Against questions):

───────────────────────────────────────────────────────────

**▶ NEXT:** `/research:discover` — [N] questions have no Direct coverage — find sources to fill them.

**Also available:**
- `/research:phase-insight` — Review which questions are thin vs. strong before deciding.
- `/research:cross-ref` — Re-run cross-reference if sources have been added since the last run.

───────────────────────────────────────────────────────────

If all questions have Direct coverage (>= 2 independent sources per question):

───────────────────────────────────────────────────────────

**▶ NEXT:** `/research:summarize-section` — Coverage is adequate — draft the phase output.

**Also available:**
- `/research:phase-insight` — Review phase strength in detail before drafting.
- `/research:cross-ref` — Confirm patterns are current before synthesis.

───────────────────────────────────────────────────────────

`What to expect:` is omitted on both paths per D-07 — next steps are unambiguous.
```

Key points:
- The existing dashboard and per-question detail (lines 87-113) are NOT changed — they already satisfy D-13 progressive disclosure.
- Two context-sensitive blocks per D-08: gaps path recommends discover, adequate path recommends summarize-section.
- No `---` separator before the blocks — the blocks are part of the Output section, not a new content section.
  </action>
  <verify>
    <automated>python3 -c "
import re
files = {
    'discover': '.claude/commands/research/discover/SKILL.md',
    'cross-ref': '.claude/commands/research/cross-ref/SKILL.md',
    'check-gaps': '.claude/commands/research/check-gaps/SKILL.md'
}
ok = True
for name, path in files.items():
    content = open(path).read()
    output_section = content.split('## Output')[1] if '## Output' in content else ''
    
    # Check next-action block exists
    if '▶ NEXT:' not in output_section:
        print(f'FAIL: {name} Output section missing next-action block')
        ok = False
    
    # Check unicode delimiters
    if '───────────────────────────────────────────────────────────' not in output_section:
        print(f'FAIL: {name} missing unicode delimiters in Output section')
        ok = False
    
    # Check no ASCII hyphen delimiters (59+)
    if re.search(r'-{59,}', output_section):
        print(f'FAIL: {name} has ASCII hyphen delimiter in Output section')
        ok = False

# discover-specific: progressive disclosure
disc = open(files['discover']).read()
disc_out = disc.split('## Output')[1] if '## Output' in disc else ''
if '| Channel | Status | Found |' not in disc_out:
    print('FAIL: discover missing channel summary dashboard table')
    ok = False

# cross-ref-specific: progressive disclosure
cr = open(files['cross-ref']).read()
cr_out = cr.split('## Output')[1] if '## Output' in cr else ''
if '| Signal | Count |' not in cr_out:
    print('FAIL: cross-ref missing signals dashboard table')
    ok = False

# check-gaps: context-sensitive blocks
cg = open(files['check-gaps']).read()
cg_out = cg.split('## Output')[1] if '## Output' in cg else ''
if '/research:discover' not in cg_out:
    print('FAIL: check-gaps missing discover recommendation in gaps path')
    ok = False
if '/research:summarize-section' not in cg_out:
    print('FAIL: check-gaps missing summarize-section recommendation in adequate path')
    ok = False

# Banned phrase check across all 3 files
banned = ['it should be noted that', 'it is worth noting', 'it may be the case that', 'one might consider', 'this would allow for', 'in order to facilitate', 'to that end']
for name, path in files.items():
    content = open(path).read()
    for phrase in banned:
        if phrase.lower() in content.lower():
            print(f'FAIL: {name} contains banned phrase: {phrase}')
            ok = False

if ok:
    print('PASS: All 3 files have correct progressive disclosure and next-action blocks')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - discover/SKILL.md Output section contains a channel summary dashboard table with columns `Channel | Status | Found`
    - discover/SKILL.md Output section contains a `---` separator between summary dashboard and detail
    - discover/SKILL.md Output section contains two `▶ NEXT:` blocks: one recommending process-source (post-discovery), one recommending cross-ref (post-batch)
    - discover/SKILL.md existing synthesis-phase block at lines 39-49 is unchanged
    - discover/SKILL.md existing Step 7 candidate presentation logic is unchanged
    - cross-ref/SKILL.md Output section contains a signals dashboard table with columns `Signal | Count`
    - cross-ref/SKILL.md Output section contains `### Cross-Reference: Phase [N]` H3 heading
    - cross-ref/SKILL.md Output section contains a `---` separator between dashboard and detail
    - cross-ref/SKILL.md Output section contains two context-sensitive `▶ NEXT:` blocks: contradictions path and clean path
    - check-gaps/SKILL.md Output section retains existing dashboard and per-question detail (lines 87-113 content preserved)
    - check-gaps/SKILL.md Output section contains two context-sensitive `▶ NEXT:` blocks: gaps path (recommends discover) and adequate path (recommends summarize-section)
    - All 3 files use unicode `───` delimiters (not ASCII hyphens) for next-action blocks
    - None of the 3 files contain any of the 8 banned phrases from CLI Tone Rules
    - No `What to expect:` line appears in any of the 6 next-action blocks (per D-07, all paths are unambiguous)
  </acceptance_criteria>
  <done>discover, cross-ref, and check-gaps all have progressive disclosure dashboards (where applicable) and context-sensitive next-action blocks using the canonical format, completing UX-01 through UX-04 across all 10 skills</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply. This plan edits documentation/instruction files (SKILL.md) that guide Claude's behavior. No user data, no API calls, no authentication, no runtime code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering | SKILL.md output instructions | accept | Files are read-only references for Claude; no user-facing runtime execution. Version controlled in git. |
</threat_model>

<verification>
1. All 5 modified files contain `▶ NEXT:` in their Output sections
2. discover and cross-ref contain progressive disclosure dashboard tables
3. check-gaps, cross-ref contain context-sensitive dual blocks
4. No file uses ASCII hyphen delimiters for next-action blocks
5. No file contains any of the 8 banned phrases
6. process-source uses bold labels only (no H3 in Output section)
7. Combined with Plan 01's work: all 10 skills now have consistent output structure per D-01 through D-16
</verification>

<success_criteria>
- All 5 SKILL.md files have next-action blocks in their Output sections
- discover/SKILL.md and cross-ref/SKILL.md have progressive disclosure dashboards with `---` separators
- check-gaps/SKILL.md and cross-ref/SKILL.md have context-sensitive dual next-action blocks
- process-source/SKILL.md uses bold labels without H3 headings
- init/SKILL.md next-action block includes `What to expect:` (D-07 exception for user-judgment steps)
- All other next-action blocks omit `What to expect:` (D-07 rule for unambiguous transitions)
- Zero banned phrases in any modified file's Output section
- All unicode delimiters verified
</success_criteria>

<output>
After completion, create `.planning/phases/16-cli-polish/16-02-SUMMARY.md`
</output>
